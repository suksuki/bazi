# 复制为部署环境片段或合并进 .env（勿提交真实口令）。
#
# 场景：后端跑在 Docker 内，PostgreSQL 跑在「宿主机」本机 5432。
# 127.0.0.1 在容器内指向容器自己，会导致 Connection refused。
# Docker Desktop / 部分环境可用 host.docker.internal 指向宿主机。

# DATABASE_URL=postgresql+psycopg2://qiazhi_admin:CHANGE_ME@host.docker.internal:5432/qiazhi_bazi?sslmode=disable

# 场景：后端与 Postgres 均为 Compose 服务且同网络，主机名用服务名（示例 postgres）：
# DATABASE_URL=postgresql+psycopg2://qiazhi_admin:CHANGE_ME@postgres:5432/qiazhi_bazi?sslmode=disable
