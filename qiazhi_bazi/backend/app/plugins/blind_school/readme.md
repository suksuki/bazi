# Blind School（`classical.blind_school.v1`）

L2 盲派做功引擎：在 **L1 物理张量** 与 **Chronos 月令 meta** 之后运行，通过 `skill_manifest.json` 声明各微模块 Skill 与 `physics_setting_key` 的绑定；审计写入 `work_vector` / chip 层，须与 L1 `skill_id` 行区分阅读。
