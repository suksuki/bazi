# Review UI 知识审阅单元 v1

状态：第一轮目录补齐

范围：记录知识、规则、合成样本失败闭环在 UI 中应展示的审阅信息。

## 治理边界

- UI 只展示失败归因、证据、draft 提案和门禁结果。
- UI 不自动批准规则，不自动启用运行时推理。
- 任何自动审批都必须可审计、可回滚、可复现。

## 第一批单元

```text
lab.review_ui_failure_loop
```

