# P0-G1.8 — External Freeze Workbench & Execution Readiness

## Objective

Turn the three remaining P0-G1.7 blockers into reproducible workbenches without allowing the machine to fabricate professional judgment, Frontier status, model preference, or a clean Git snapshot.

## Deliverables

### Human Expert Reference

- Ten-chart authoring workspace with authoritative chart-fact hashes.
- Required professional dimensions for pattern, alternatives, work paths, conditional roles, portrait, career and wealth priors.
- Per-item reason, evidence basis and source references.
- Recursive prohibition of Reality Evidence, model outputs and Lane outputs.
- Strict human signature and freeze validator.
- Versioned frozen-candidate writer; silent edits remain prohibited.

### Frontier selection

- Reproducible candidate policy contract.
- Five isolated selection charts, three repeats per candidate.
- Direct strong power-user prompt only.
- No formal/development charts, Expert Reference, Reality Evidence or DeepBazi internal protocol.
- Immutable raw outputs, blind packet, separate operator map and human selection.
- No automatic winner and no schema-only promotion.

### Execution snapshot

- Phase 0 source file manifest and SHA-256 root hash.
- Git commit and V50 scope status.
- Refusal to freeze a dirty or untracked working tree.
- No automatic commit and no modification of unrelated V40 work.

## Completion semantics

`machine_workbench_complete_external_inputs_pending` is a successful machine outcome. It means every remaining gate has a concrete path to completion. It does not mean P0-G2 is ready.

P0-G2 remains prohibited until:

```text
Human Expert Reference frozen
+ true Frontier policy selected and frozen
+ clean committed execution snapshot
+ explicit approval for final non-sealed preflight
```

## Reproduce

```bash
cd /Users/liujin/DEV/AIProjects/bazi/qiazhi/v50
PYTHONPATH=.:packages:apps ../.venv312/bin/python scripts/v50_prepare_vnext_phase0_g1_8.py
```
