# 老系统（Antigravity / FDS）

本目录包含**全部**老代码与资源：`core/`、`config/`、`ui/`、`registry/`、`docs/`、`scripts/`、`tests/`、`data_local/` 等。

- **入口**：`legacy/main.py`（Streamlit，仅此文件启动老界面）
- **API**：`api/app.py`（`GET /api/v2/...`）

启动方式见仓库根目录 `REPO_LAYOUT.md`。
