# Python 3.12+（Qiazhi / V17 与启动脚本对齐；见 qiazhi/.python-version）
# FDS 严格模式（Manifest 法律全文校验）— 第 019 号工程指令
# 注：PyPI 包名为 json-logic-qubit（提供 json_logic.jsonLogic）
json-logic-qubit

# 第 045 号：FDS 双核 — DuckDB 特征计算层
duckdb

# FDS 2.0 API（流形追踪 GET /api/v2/manifold/trace/{user_id}）
fastapi
uvicorn[standard]

# Qiazhi-Bazi：元数据协议（Pydantic v2）
pydantic>=2.0

# 其他核心依赖（按需补充，如 streamlit / numpy / ollama 等）
# streamlit
# numpy
# ollama

httpx

sqlmodel
psycopg[binary]
psycopg2-binary
lunar_python
