# QUANTUM TRINITY V2.0: 架构重组与大清扫报告

## ⚖️ 整体综述 (Overview)
为了彻底解决 V9.3 系统中模型碎片化和逻辑重叠的问题，我们执行了“大清扫”，建立了 **Quantum Trinity V2.0** 架构。新架构采用了**分层物理建模**，将 18 个散乱文件压缩为 4 个核心动力学集群，确保了高度的模块耦合性与算法解释力。

## 🏗️ 新框架 (The New Framework)
新架构分为四个逻辑层，形成了“从底层物理到高层真言”的完整链条：

1.  **Nexus 层 (核心资产)**:
    *   `nexus/definitions.py`: 统一了干支相位、五行常数与判定优先级作为 Single Source of Truth。
2.  **Physics 层 (物理律令)**:
    *   `physics/wave_laws.py`: 封装了波函数叠加、非线性饱和与 Landau 相变理论。
3.  **Engines 层 (动力学集群)**:
    *   `engines/energy_flux.py`: 模拟“流年/大运”与命局间的能量流变（Rheology）。
    *   `engines/resonance_field.py`: 负责从格判定、脆断风险及空亡惩罚的统一计算。
4.  **Intelligence 层 (逻辑裁决)**:
    *   `intelligence/logic_arbitrator.py`: 负责地支冲合会的仲裁协议与初始场初始化。

## 🧪 新算法 (New Algorithms)
*   **流变模拟 (Flux Rheology)**: 弃用了简单的线性传递，改用基于复数阻抗的流速模型，动态计算母子元素间的导通效率。
*   **相变参量化 (Landau Order Parameter)**: 使用统计物理中的序参量（$\eta$）来量化身强身弱，实现了从连续波强到离散标签的平滑坍缩。
*   **统一共振评分 (Unified Q-Score)**: 引入 Quality Factor (Q) 作为交互强度的唯一量化指标，替代了以往模糊的评分系数。

## 📜 新规则 (New Bazi Rules)
*   **强制仲裁协议 (Arbitration Protocol)**: 严格执行 `三会 > 三合 > 六合 > 冲 > 害` 的物理锁定顺序。一旦高优结构锁定支点，低优干扰将表现为“阻抗偏置”而非逻辑冲突。
*   **空亡黑洞效应 (Void Singularity)**: 在算法底层赋予空亡支点 $0.3$ 的能量屏蔽因子，导致即使符合从格条件，能量也会因底层虚空而无法形成超导态。

## ⚙️ 新参数 (New Parameters)
*   `BRITTLENESS_COEFF`: $0.85$ (脆性触发阈值)。
*   `SYNC_THRESHOLD_COHERENT`: $0.85$ (超导判定)。
*   `PILLAR_WEIGHTS`: `Month (3.0)`, `Annual (1.5)`, `Luck (1.2)` - 提升了时间线矢量的权重。

---
**状态**: ✅ 量子真言架构已重塑 | 冗余文件已归档 | 验证脚本已通过 (Core Flow)
**下一步**: 基于 V2.0 框架开展“伤官见官”的高能对冲实验。
