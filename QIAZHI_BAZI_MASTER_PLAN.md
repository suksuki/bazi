# Qiazhi-Bazi（掐指八字）总体规划与设计大纲 v1.0

> 本文档为 Qiazhi-Bazi 后续全部开发工作的**北极星**：描述愿景与架构原则，**不绑定具体代码路径或实现细节**。

---

## 1. 项目愿景 (Core Vision)

* **定位**：一个 **AI 原生（AI-Native）** 的进化型命理推演系统；与同仓库中的历史 Python 引擎（legacy）**无叙事上的从属关系**。
* **核心理念**：放弃对绝对定量的追求，通过「物理模拟 + 逻辑抽丝剥茧 + 交互式上演化」来理解命运。
* **终极目标**：构建一个能够自我学习、自我修正逻辑阈值的「命理专家系统」。

### 1.1 心智模型：默认「忘记老系统」

* **产品、文档与日常开发**以 **Qiazhi-Bazi（`qiazhi/`、`qiazhi_core`）** 为唯一叙事中心；**不**默认讨论、修改或依赖老界面与老业务流。
* **仅在必要时**引用 legacy：例如需要复用已验证的历法/排盘/物理配置、对齐既有协议常量、或做数据对照时——**按需打开**，而非把老系统当作前置条件。
* 技术实现上若通过 Bridge 读取 legacy 数据，属于**可选集成**，不改变「掐指八字」独立产品边界。

---

## 2. 核心架构设计 (System Architecture)

系统采用 **「绞杀者模式」**：在演进过程中，以 **`qiazhi_core` 为真源** 逐步承接能力；同仓 **legacy** 可作为数据源与算力借用方，**不**作为产品定义来源。

### 2.1 模块化分层

1. **数据解耦层 (Bridge)**：将标准干支、节气及基础五行等输入转化为新系统的标准协议；**必要时**从同仓 legacy 抽取或对照，**不**强制耦合。
2. **物理逻辑插件 (Logic Plugins)**：将《Antigravity V3.0》协议抽象为独立插件（如：墓库状态、能量传导、格局拓扑）。
3. **交互推演引擎 (Reasoning Engine)**：维护推演状态机，处理从「基础判定」到「深度破案」的流转。
4. **进化层 (Evolutionary Layer)**：存储决策链数据，利用本地 LLM 进行离线审计与模拟测算，实现规则演化。

---

## 3. 三位一体交互模型 (Interaction Model)

这是系统的核心运行模式，定义了三个角色的协同方式：

* **角色 A：项目裁决人 (Arbiter)**
  * **职责**：总体路径的裁决者。
  * **动作**：在「教练模式」下对逻辑矛盾点进行勾选（打钩），确认推理路径。

* **角色 B：本地 LLM (The Analyst)**
  * **职责**：翻译官、引导者、审计员。
  * **动作**：向裁决人提供选项建议；向用户发起生活化的事实引导提问；在空闲时进行自我对弈演化。

* **角色 C：系统 (The System)**
  * **职责**：规则维护者、物理计算器。
  * **动作**：执行底层的能量传导计算；生成基于盘面的逻辑矛盾列表；记录所有决策数据。

---

## 4. UI/UX 哲学

* **形态**：Web 应用（非 APP），但必须极致适配手机端操作。
* **模式**：**引导式、渐进式**。不一次性展示结论，而是通过来回交互，像「破案」一样一步步揭示真相。
* **多语言**：底层原生支持 **中、英、韩** 三种语言，确保逻辑描述的跨文化一致性。

---

## 5. 进化与验证机制 (Validation & Evolution)

* **反向推导验证**：通过「合成八字」或已知转折点的名人八字进行盲测，验证推理路径的一致性。
* **全息演化**：利用 51 万全息八字案例，结合 LLM 进行自动测算，发现逻辑漏洞并自动建议调整物理阈值（如判定入格的马氏距离阈值）。

---

## 6. 实施路线图 (Roadmap)

1. **Phase 1**：环境搭建与协议定义（定义 `BaziMetadata`）。
2. **Phase 2**：教练模式原型（实现第一个「打钩」确认界面）。
3. **Phase 3**：双向诱导提问流（实现 LLM 对外与对内的对话逻辑）。
4. **Phase 4**：离线演化系统（开启全息案例审计）。

---

## 7. 下一步（与实现衔接）

第一步实现重点为 **`BaziMetadata` 协议**：把盘面与推演所需的结构信息，表达为新系统与 LLM 共读的**语义特征**；与 Bridge、插件层对齐。若需从 legacy 侧对齐字段，**仅作为映射参考**，不反向把老系统实现细节写进协议正文。

（物理参数与阈值仍须遵守仓库「零硬编码」公理：仅从配置或 `AlgoParams` 注入。）

---

## 8. Agent Skills（协作规范增量）

* 仓库内 **`.cursor/skills/`** 存放可随版本迭代的任务级说明（每个 Skill 为独立目录 + `SKILL.md`）。
* **索引与命名**：见 **`.cursor/skills/README.md`**；**计划中的 Skill**：见 **`.cursor/skills/ROADMAP.md`**（分阶段勾选、逐步补充）。
* Qiazhi 相关能力建议使用 **`qiazhi-`** 前缀新建 Skill，与 **`fds-`**（legacy/FDS）并列，避免混在同一 Skill 中。
