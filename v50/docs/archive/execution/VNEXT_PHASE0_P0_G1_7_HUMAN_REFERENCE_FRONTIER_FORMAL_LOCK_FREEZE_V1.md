# VNext Phase 0 P0-G1.7 — Human Reference, Frontier Policy & Formal Lock Freeze

## Status

This slice closes the two policy decisions raised in P0-G1.6 and prepares the remaining human/external gates. It does not evaluate professional cognition and cannot start P0-G2.

## Frozen decisions

### Direct baseline

Direct Same Model and Direct Frontier use one strong professional power-user request. They receive pillars, gender, and desired output targets only. They do not receive DeepBazi's staged synthesis protocol, facts, tools, retrieval, examples, or review artifacts.

### P0 repair authority

P0 allows mechanical JSON/schema recovery only. Hypotheses, work paths, functional roles, domain claims, causality, confidence, and counterevidence cannot be rewritten. The typed cognitive output is immutable; machine review is stored as a separate annotation. Production delivery repair remains unchanged.

### Modality taxonomy

The fact gate recognizes seven modalities:

```text
asserted_natal_fact
derived_natal_claim
hypothesis
counterfactual
timing_condition
question
quoted_claim
```

Only the first two may trigger immutable natal-fact conflicts. Questions may receive a separate premise-quality review without being counted as model fact assertions.

## Remaining workstreams

1. Human-freeze the ten-chart Expert Reference Space without model outputs or Reality Evidence.
2. Select a reproducible true Frontier policy using only the five isolated selection charts.
3. Create a clean committed V50 execution snapshot.
4. After all three close, request explicit human approval for one final non-sealed live preflight.

## Red lines

- No sealed ten-chart execution.
- No P0-G2 start.
- No LLM-authored Expert Reference.
- No convenient local model relabeled as Frontier.
- No semantic repair of P0 outputs.
- No training, weight change, theory change, UI change, Abu change, or Discovery Lab expansion.

## Reproduce

```bash
cd /Users/liujin/DEV/AIProjects/bazi/qiazhi/v50
PYTHONPATH=.:packages:apps ../.venv312/bin/python scripts/v50_prepare_vnext_phase0_g1_7.py
```

The run must end with `ready_for_p0_g2: false` until all external gates and explicit approval are present.
