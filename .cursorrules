# Antigravity / Qiazhi-Bazi — 协作规则

## Qiazhi 优先（忘记老系统为默认心智）

- **默认**以 `qiazhi/`、`qiazhi_core` 为叙事与改动的中心；**不要**默认假设要改 `legacy/` 或讨论老 Streamlit 界面。
- **仅在任务明确要求**或**确需复用历法/排盘/配置/对照数据**时，再打开 `legacy/` 并引用；参见根目录 `QIAZHI_BAZI_MASTER_PLAN.md` §1.1。

## 物理与结构（仍适用于全仓）

1. **宪法与零硬编码**：算法须符合 `legacy/docs/ALGORITHM_CONSTITUTION_v3.0.md` 等规范；权重、系数、阈值只从 `legacy/config` 或配置对象读取，不在 `.py` 中写死物理常数。
2. **MVC**：`legacy/ui/` 只渲染；`legacy/core/` 只计算；Qiazhi 前端不塞进 legacy UI。
3. **概率输出**：判定优先用 score / level / meta，避免简单 True/False。
4. **界面语言**：面向用户的 Streamlit 文案用简体中文。

## V17_MANDATES（永久锚定）

1. **叙事管线唯一合法路径**：`NarrativeSanitizer -> SemanticFusion(Micro-LLM) -> payload.render_text`。任何绕过管线的叙事生成都视为违规。
2. **意志坍缩强约束**：所有实时判词必须显式接收 `will_proxy` 并做语气偏置，不允许输出“中性模板句”冒充实时极化。
3. **禁止工程词上屏**：`VF.`、`Abs`、`Fact_ID`、`seed`、`node_id`、`verified_fact_lines` 等词汇不得出现在用户可见文本中。
4. **协议锁**：前端只能消费 `payload.render_text`。无该字段的帧必须视为无效信号并丢弃。
5. **首席法官执行权**：若代码尝试违反以上 V17 宪法，必须在编辑中标注错误并拒绝生成该违规实现。
