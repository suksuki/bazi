# V21.0 任务 57：核心物理定律与耦合效应的集成部署方案

## 📋 部署概述

**目标：** 彻底稳定模型的第一层（十神波函数计算），消除系统性偏差。

**状态：** 📝 部署方案（待实现）

---

## 第一部分：参数部署（覆盖）

### 1.1 基础场域（Physics）参数更新

| 参数名 | 当前值 | V21.0 理论初始值 | 理论基础 |
|--------|--------|------------------|----------|
| **pillarWeights.year** | 0.8 | **1.0** | 年柱权重：祖上，影响最弱 |
| **pillarWeights.month** | 1.2 | **2.0** | 月令权重：提纲，权衡全局（最重要） |
| **pillarWeights.day** | 1.0 | **1.5** | 日柱权重：自身，能量集中 |
| **pillarWeights.hour** | 0.9 | **1.2** | 时柱权重：归宿，影响次之 |

### 1.2 粒子动态（Structure）参数更新

**注意：** 这些参数在 `config/parameters.json` 中可能不存在，需要在 `core/config_schema.py` 的 `DEFAULT_FULL_ALGO_PARAMS` 中定义，或通过引擎配置传递。

| 参数名 | 当前值 | V21.0 理论初始值 | 理论基础 |
|--------|--------|------------------|----------|
| **rootingWeight** | 1.0 | **1.5** | 通根系数：天干在地支藏干中出现 |
| **exposedBoost** | 1.5 | **1.3** | 透干加成：地支能量显露天干 |
| **samePillarBonus** | 1.2 | **2.0** | 自坐强根：羊刃、建禄等最强支撑 |

### 1.3 几何交互（Interactions）参数更新

**注意：** 这些参数在 `config/parameters.json` 中可能不存在，需要在 `interactions` 模块中定义。

| 参数名 | 当前值 | V21.0 理论初始值 | 理论基础 |
|--------|--------|------------------|----------|
| **comboPhysics.directionalBonus** | 3.0 | **4.0** | 三会倍率：力量最强，改变全局 |
| **comboPhysics.trineBonus** | 2.5 | **3.0** | 三合倍率：凝聚力量，影响深远 |
| **branchEvents.clashScore** | -5.0 | **-15.0** | 六冲惩罚：破坏性强，直接扣分 |
| **branchEvents.clashDamping** | 0.3 | **0.7** | 冲的折损：六冲时的能量折损系数 |
| **comboPhysics.resolutionCost** | 0.1 | **0.4** | 解冲消耗：贪合忘冲，能量消耗 |

### 1.4 能量流转（Flow）参数更新

| 参数名 | 当前值 | V21.0 理论初始值 | 理论基础 |
|--------|--------|------------------|----------|
| **flow.controlImpact** | 0.5 | **0.7** | 克-打击力：官杀攻身的强度 |
| **flow.resourceImpedance.base** | 0.75 | **0.15** | 基础阻抗：印星（生我）的基础吸收/阻抗 |
| **physics.InteractionFactors.Consume_Factor** | 0.90 | **0.9** | 财星消耗：财星（我克）的消耗作用（保持不变） |

### 1.5 能量流转（Flow）新增参数

| 参数名 | V21.0 理论初始值 | 理论基础 |
|--------|------------------|----------|
| **flow.couplingEffects.Sequential_Gain_Factor** | **1.20** | 连续顺生放大倍率 |
| **flow.couplingEffects.Coherence_Boost_Factor** | **0.15** | 合力共振加成倍率 |
| **flow.couplingEffects.Cancellation_Factor** | **0.60** | 多重力量抵消比例 |

---

## 第二部分：算法集成（强制）

### 2.1 复杂交互集成

**目标：** 将合、冲、刑、害、墓库、禀赋特性等逻辑从流年计算中提升到 PhysicsProcessor，直接修正 raw_energy。

**需要集成的模块：**
1. **HarmonyEngine** - 三合、六合、六冲
2. **TreasuryEngine** - 墓库物理
3. **天干五合** - 从 HarmonyEngine 中提取
4. **相刑相害** - 从 interactions.py 中提取

**实现位置：**
- 在 `PhysicsProcessor.process()` 中，在计算完基础能量、通根加成、时代修正之后
- 在返回 raw_energy 之前
- 直接修正 `raw_energy` 字典

**实现逻辑：**
```
1. 检测天干五合（年-月、月-日、日-时）
   - 如果合化成功：增加目标元素能量，减少原始元素能量
   - 如果合绊：减少原始元素能量

2. 检测地支三合/三会
   - 如果形成三合/三会：增强对应元素能量

3. 检测地支六合
   - 如果形成六合：增强对应元素能量

4. 检测地支六冲
   - 如果形成六冲：减少被冲元素的能量

5. 检测墓库
   - 如果日支是墓库：根据能量阈值判断是库还是墓
   - 如果是库且能量高：增强对应元素能量
   - 如果是墓：减少对应元素能量

6. 检测相刑相害
   - 如果形成相刑：减少相关元素能量
   - 如果形成相害：减少相关元素能量
```

### 2.2 系统能量流与耦合集成

**目标：** 实现顺生链放大、合力共振和多重力量抵消算法，修正 raw_energy 在四柱间传递时的动态变化。

**实现位置：**
- 在 `PhysicsProcessor.process()` 中，在复杂交互集成之后
- 在返回 raw_energy 之前
- 直接修正 `raw_energy[dm_element]`

**实现逻辑：**
```
1. 顺生链放大（Sequential Amplification）
   - 检测年→月→日的顺生链
   - 如果存在：日主能量 *= (Sequential_Gain_Factor ^ 链长度)

2. 合力共振（Coherent Resonance）
   - 统计所有生日干的天干数量：N
   - 如果 N >= 2：日主能量 *= (1 + (N - 1) * Coherence_Boost_Factor)

3. 多重力量抵消（Multi-Force Cancellation）
   - 统计生日干的天干数量：N_support
   - 统计克日干的天干数量：N_control
   - 如果 N_support > 0 且 N_control > 0：
     - 净能量系数 = (N_support - N_control * Cancellation_Factor) / (N_support + N_control)
     - 日主能量 *= max(净能量系数, 0.1)
```

---

## 第三部分：冻结确认

### 3.1 第二层参数冻结（V18.0 稳定值）

**确认以下参数保持不变：**

```json
{
  "ObservationBiasFactor": {
    "Wealth": 2.7,
    "CareerBiasFactor_LowE": 2.0,
    "CareerBiasFactor_HighE": 0.95,
    "Relationship": 3.0,
    "CaseSpecificBias": {
      "C02": 2.682,
      "C07": 1.338
    }
  },
  "physics": {
    "SpacetimeCorrector": {
      "CorrectorBaseFactor": 1.0,
      "LuckPillarWeight": 0.6,
      "AnnualPillarWeight": 0.4,
      "Enabled": true,
      "ExclusionList": ["C01", "C02", "C07"],
      "CaseSpecificCorrectorFactor": {
        "C03": 1.464,
        "C04": 3.099,
        "C06": 0.786,
        "C08": 0.900
      }
    },
    "WealthAmplifier": 1.2,
    "CareerAmplifier": 1.2,
    "RelationshipAmplifier": 1.0
  }
}
```

**状态：** ✅ 已冻结，不参与本轮调优

---

## 第四部分：验证计划

### 4.1 批量校准验证

**执行步骤：**
1. 运行 `scripts/run_batch_calibration.py`
2. 记录所有 8 个案例的 MAE
3. 对比 V20.0 的 MAE 结果
4. 分析变化趋势

### 4.2 重点关注案例

**C07 (事业)：**
- V20.0 MAE: 9.3（模型=70.7, GT=80.0）
- **目标：** MAE < 5.0
- **预期：** 通过能量流耦合修正，可能提升日主能量，从而改善事业相得分

### 4.3 验证指标

| 指标 | 目标 | 说明 |
|------|------|------|
| **整体成功率** | ≥ 75% (6/8) | 至少 6 个案例 MAE < 5.0 |
| **C07 MAE** | < 5.0 | 重点关注案例 |
| **平均 MAE** | < 5.0 | 所有案例的平均 MAE |

---

## 第五部分：实现检查清单

### 5.1 参数部署检查

- [ ] 更新 `config/parameters.json` 中的 `pillarWeights`
- [ ] 更新或添加 `flow` 模块中的参数
- [ ] 更新或添加 `interactions` 模块中的参数（如果存在）
- [ ] 确认所有理论初始值已正确设置

### 5.2 算法集成检查

- [ ] 在 `PhysicsProcessor` 中集成复杂交互逻辑
- [ ] 在 `PhysicsProcessor` 中实现能量流耦合效应
- [ ] 确保算法在正确的时机执行（在通根加成、时代修正之后）
- [ ] 确保算法直接修正 `raw_energy` 字典

### 5.3 引擎配置检查

- [ ] 在 `EngineV88` 中传递 `flow_config` 给 `PhysicsProcessor`
- [ ] 在 `EngineV91` 中传递 `flow_config` 给 `PhysicsProcessor`
- [ ] 确保 `interactions_config` 正确传递（如果存在）

### 5.4 验证检查

- [ ] 运行批量校准脚本
- [ ] 记录所有案例的 MAE
- [ ] 生成验证报告
- [ ] 分析 C07 的收敛情况

---

## 第六部分：预期影响分析

### 6.1 参数变化的影响

**pillarWeights 变化：**
- 月令权重从 1.2 提升到 2.0：**大幅增强月令的影响**
- 日柱权重从 1.0 提升到 1.5：**增强日主自身能量**
- 时柱权重从 0.9 提升到 1.2：**增强时柱的影响**

**预期效果：**
- ✅ 更准确地反映"月令最强"的命理原则
- ✅ 增强日主能量的计算精度
- ⚠️ 可能导致部分案例的能量过度放大

### 6.2 算法集成的影响

**复杂交互集成：**
- ✅ 天干五合、三合、六合、六冲、墓库等逻辑直接修正基础能量
- ✅ 更完整的第一层计算
- ⚠️ 可能改变所有案例的基础能量分布

**能量流耦合：**
- ✅ 顺生链放大：增强连续顺生的能量传递
- ✅ 合力共振：增强多力合一的效应
- ✅ 多重抵消：更准确地模拟生克并存的状态
- ⚠️ 可能显著改变日主能量，影响所有维度得分

### 6.3 整体预期

**乐观情况：**
- 所有案例 MAE < 5.0
- C07 事业相 MAE 显著改善

**保守情况：**
- 部分案例 MAE 改善，部分恶化
- 需要重新调优第二层参数（BiasFactor、CorrectorFactor）

**风险情况：**
- 整体 MAE 上升
- 需要调整 V21.0 理论初始值或算法逻辑

---

## 第七部分：回滚方案

### 7.1 如果整体 MAE 显著上升

**回滚步骤：**
1. 恢复 `config/parameters.json` 到 V20.0 版本
2. 移除 `PhysicsProcessor` 中的复杂交互和能量流耦合逻辑
3. 重新运行批量校准验证

### 7.2 如果部分案例恶化

**调整策略：**
1. 保持算法集成不变
2. 微调 V21.0 理论初始值
3. 必要时重新调优第二层参数

---

---

## 第八部分：参数更新记录

### 8.1 已更新的参数（config/parameters.json）

**✅ 基础场域（Physics）：**
- `pillarWeights.year`: 0.8 → **1.0**
- `pillarWeights.month`: 1.2 → **2.0**
- `pillarWeights.day`: 1.0 → **1.5**
- `pillarWeights.hour`: 0.9 → **1.2**

**✅ 能量流转（Flow）：**
- `resourceImpedance.base`: 0.75 → **0.15**
- `controlImpact`: 新增 **0.7**
- `couplingEffects`: 新增
  - `Sequential_Gain_Factor`: **1.20**
  - `Coherence_Boost_Factor`: **0.15**
  - `Cancellation_Factor`: **0.60**

**✅ 几何交互（Interactions）：**
- `comboPhysics.trineBonus`: 新增 **3.0**
- `comboPhysics.directionalBonus`: 新增 **4.0**
- `comboPhysics.resolutionCost`: 新增 **0.4**
- `branchEvents.clashDamping`: 新增 **0.7**
- `branchEvents.clashScore`: 新增 **-15.0**

### 8.2 待实现的参数（需要在代码中支持）

**⚠️ 粒子动态（Structure）：**
- `rootingWeight`: 需要在引擎配置中支持（理论值：1.5）
- `exposedBoost`: 需要在引擎配置中支持（理论值：1.3）
- `samePillarBonus`: 需要在引擎配置中支持（理论值：2.0）

**注意：** 这些参数可能需要在 `core/config_schema.py` 的 `DEFAULT_FULL_ALGO_PARAMS` 中定义，或通过引擎配置传递。

---

**文档生成时间：** V21.0 Task 57 (Deployment Plan)
**文档状态：** 📝 部署方案（参数已更新，算法待实现）

