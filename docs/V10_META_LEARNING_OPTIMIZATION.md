# V10.0 元学习调优体系 (Meta-Learning Optimization)

**版本**: V10.0  
**发布日期**: 2025-12-17  
**状态**: ✅ 正式发布

---

## 📋 目录

1. [概述](#概述)
2. [核心问题](#核心问题)
3. [五大调优策略](#五大调优策略)
4. [元学习工作流](#元学习工作流)
5. [实现细节](#实现细节)
6. [案例：Jason D 1999年误差修正](#案例jason-d-1999年误差修正)

---

## 概述

在 **Antigravity V10.0** 的非线性架构下，传统的线性调优（如简单的梯度下降）已经无法捕捉高维、非凸的能量场变化。V10.0 引入了**元学习 (Meta-Learning)** 体系，实现智能参数调优。

### 核心突破

1. **从梯度下降到贝叶斯优化**: 使用代理模型和期望改进来寻找全局最优解
2. **超参数敏感度分析**: 分析激活函数参数对预测结果的影响
3. **对比学习 RLHF**: 提供"路径 A vs 路径 B"的对比，而非单一反馈
4. **Transformer 位置编码调优**: 平衡"远期积压能量"与"近期突发能量"
5. **GAT 路径过滤**: 过滤无效注意力路径，聚焦核心路径

---

## 核心问题

### 传统调优的局限性

**V9.3 及之前版本**:
- ❌ 使用简单的梯度回归
- ❌ 损失函数假设为凸函数
- ❌ 无法处理高维参数空间
- ❌ 容易陷入局部最优

**V10.0 非线性架构的挑战**:
- ✅ 损失函数极其复杂（非凸、高维）
- ✅ 激活函数参数极度敏感
- ✅ 需要平衡多个目标（准确性、不确定性、路径合理性）
- ✅ 需要理解能量传播的长期趋势

---

## 五大调优策略

### 1. 贝叶斯优化 (Bayesian Optimization)

#### 核心思想

使用**高斯过程（Gaussian Process）**作为代理模型，通过**期望改进（Expected Improvement）**来寻找最优参数组合。

#### 数学原理

**高斯过程**:
```
f(x) ~ GP(μ(x), k(x, x'))
```

其中：
- `μ(x)`: 均值函数
- `k(x, x')`: 协方差函数（核函数）

**期望改进（EI）**:
```
EI(x) = E[max(0, f_min - f(x))]
```

其中：
- `f_min`: 当前最优值
- `f(x)`: 预测值

**采集函数**:
```
x_next = argmax_x EI(x)
```

#### 优势

- ✅ 适合处理非凸、高维优化问题
- ✅ 充分利用已有观测数据
- ✅ 能够处理不确定性（置信区间）
- ✅ 高效找到全局最优解

#### 实现

```python
from core.bayesian_optimization import BayesianOptimizer

# 定义参数边界
parameter_bounds = {
    'strength_beta': (5.0, 15.0),
    'clash_k': (3.0, 7.0),
    'trine_boost': (0.1, 0.5),
    'tunneling_factor': (0.05, 0.2)
}

# 创建优化器
optimizer = BayesianOptimizer(
    parameter_bounds=parameter_bounds,
    acquisition_func='ei',  # 期望改进
    n_initial_samples=10
)

# 执行优化
optimal_params = optimizer.optimize(objective_func, n_iterations=50)
```

---

### 2. 超参数敏感度分析 (Hyperparameter Sensitivity Analysis)

#### 核心思想

分析非线性激活函数参数（如 Softplus 的 `β`、Sigmoid 的 `k`）对预测结果的敏感度，识别关键参数。

#### 数学原理

**敏感度定义**:
```
S_i = ∂L/∂θ_i
```

其中：
- `L`: 损失函数
- `θ_i`: 第 i 个参数

**梯度计算**:
```
S_i ≈ (L(θ_i + ε) - L(θ_i - ε)) / (2ε)
```

#### 分析流程

1. 固定其他参数，只改变目标参数
2. 计算损失函数的变化
3. 绘制敏感度曲线
4. 识别最优参数值

#### 实现

```python
from core.bayesian_optimization import HyperparameterSensitivityAnalyzer

# 创建分析器
analyzer = HyperparameterSensitivityAnalyzer(base_params)

# 分析单个参数
result = analyzer.analyze(
    objective_func=objective,
    parameter_name='strength_beta',
    range_values=np.linspace(5.0, 15.0, 20)
)

# 获取最优值
optimal_value = result['optimal_value']
```

---

### 3. 对比学习 RLHF (Contrastive RLHF)

#### 核心思想

不仅告诉系统"预测错了"，还提供"路径 A vs 路径 B"的对比，让系统学习哪种能量传导逻辑更符合真实事件。

#### 数学原理

**Bradley-Terry 模型**:
```
P(A > B) = exp(r_A) / (exp(r_A) + exp(r_B))
```

其中：
- `r_A, r_B`: 路径 A 和 B 的奖励值

**对比学习损失**:
```
L = -log(P(preferred_path))
```

#### 优势

- ✅ 提供相对偏好，而非绝对反馈
- ✅ 能够学习复杂的能量传播模式
- ✅ 自动识别 GAT 注意力头的权重分配

#### 实现

```python
from core.contrastive_rlhf import ContrastiveRLHFTrainer, ContrastiveRewardModel

# 创建奖励模型
reward_model = ContrastiveRewardModel()

# 创建训练器
trainer = ContrastiveRLHFTrainer(reward_model)

# 生成对比学习对
pairs = trainer.generate_contrastive_pairs(
    case_data=case_data,
    engine_a=engine_a,  # 不同参数配置
    engine_b=engine_b,
    target_years=[1999, 2015, 2021]
)

# 训练奖励模型
reward_model.train(pairs, n_epochs=100)
```

---

### 4. Transformer 位置编码调优 (Positional Encoding Tuning)

#### 核心思想

调整 Transformer 的位置编码参数，平衡"远期积压能量"与"近期突发能量"的权重。

#### 数学原理

**位置编码**:
```
PE(pos, 2i) = sin(pos × decay_factor / (10000^(2i/d_model)))
PE(pos, 2i+1) = cos(pos × decay_factor / (10000^(2i/d_model)))
```

其中：
- `decay_factor`: 衰减因子（影响远期能量的权重）

**多尺度时序融合**:
```
fused = w_short × f_short + w_medium × f_medium + w_long × f_long
```

其中：
- `w_short, w_medium, w_long`: 不同时间尺度的权重

#### 调优目标

- 识别 Jason D 的财富爆发是否受 50 年前的"燥土"影响
- 还是受近 10 年的大运场驱动
- 通过多尺度时序融合自动参数博弈

#### 实现

```python
from core.transformer_position_tuning import PositionalEncodingTuner, MultiScaleTemporalFusion

# 创建位置编码调优器
tuner = PositionalEncodingTuner(d_model=128, max_length=100)

# 调优位置编码参数
optimal_params = tuner.tune_for_long_range_dependency(
    timeline_data=timeline_data,
    objective_func=objective
)

# 多尺度时序融合
fusion = MultiScaleTemporalFusion()
optimal_weights = fusion.optimize_scale_weights(
    timeline_data=timeline_data,
    ground_truth=ground_truth,
    objective_func=objective
)
```

---

### 5. GAT 路径过滤 (GAT Path Filtering)

#### 核心思想

过滤掉对财富指数贡献低于阈值的无效注意力路径，使核心路径（如"未→丑"）更加聚焦。

#### 数学原理

**路径强度**:
```
strength(path_i→j) = attention_weight(i, j) × energy_flow(i, j)
```

**路径过滤**:
```
filtered_paths = {path | strength(path) >= threshold}
```

**系统熵控制**:
```
H = -Σ p_i × log(p_i)
```

其中：
- `H`: 路径熵
- `p_i`: 路径 i 的权重

#### 调优目标

- 过滤微弱的能量干扰（熵）
- 聚焦核心路径（如"未→丑"）
- 通过调整 `systemEntropy` 参数控制路径数量

#### 实现

```python
from core.gat_path_filter import GATPathFilter, SystemEntropyController

# 创建路径过滤器
filter = GATPathFilter(threshold=0.1)

# 过滤路径
filtered_weights = filter.filter_paths(
    attention_weights=attention_weights,
    energy_paths=energy_paths
)

# 优化阈值
optimal_threshold = filter.optimize_threshold(
    attention_weights=attention_weights,
    energy_paths=energy_paths,
    objective_func=objective
)

# 系统熵控制
entropy_controller = SystemEntropyController(base_entropy=0.1)
filtered_by_entropy = entropy_controller.filter_by_entropy(
    attention_weights=attention_weights,
    max_entropy=2.0
)
```

---

## 元学习工作流

### 完整调优流程

```
┌─────────────────────────────────────────────────────────┐
│ 步骤1: 识别高不确定性、高误差的年份                      │
│  - 使用贝叶斯推理的不确定性输出                          │
│  - 锁定需要优化的年份（如 1999 年）                      │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 步骤2: 超参数敏感度分析                                  │
│  - 分析各个参数对预测结果的影响                          │
│  - 识别关键参数（如 strength_beta, clash_k）            │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 步骤3: 贝叶斯优化                                        │
│  - 使用高斯过程作为代理模型                              │
│  - 通过期望改进寻找最优参数组合                          │
│  - 调整非线性激活函数的斜率                              │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 步骤4: 对比学习 RLHF                                     │
│  - 生成对比学习对（路径 A vs 路径 B）                    │
│  - 训练奖励模型                                          │
│  - 优化 GAT 注意力头的权重分配                           │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 步骤5: Transformer 位置编码调优                         │
│  - 调整位置编码参数                                      │
│  - 优化多尺度时序融合权重                                │
│  - 平衡远期与近期能量的贡献                              │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 步骤6: GAT 路径过滤                                      │
│  - 优化路径过滤阈值                                      │
│  - 控制系统熵                                            │
│  - 聚焦核心能量路径                                      │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 步骤7: 验证与迭代                                        │
│  - 验证优化后的参数                                      │
│  - 检查预测准确性                                        │
│  - 如果未达到目标，返回步骤2                              │
└─────────────────────────────────────────────────────────┘
```

---

## 实现细节

### 核心模块

1. **core/bayesian_optimization.py**
   - `GaussianProcess`: 高斯过程代理模型
   - `BayesianOptimizer`: 贝叶斯优化器
   - `HyperparameterSensitivityAnalyzer`: 敏感度分析器

2. **core/contrastive_rlhf.py**
   - `ContrastiveRewardModel`: 对比奖励模型
   - `ContrastiveRLHFTrainer`: 对比学习训练器
   - `PredictionPath`: 预测路径数据类

3. **core/transformer_position_tuning.py**
   - `PositionalEncodingTuner`: 位置编码调优器
   - `MultiScaleTemporalFusion`: 多尺度时序融合

4. **core/gat_path_filter.py**
   - `GATPathFilter`: GAT 路径过滤器
   - `SystemEntropyController`: 系统熵控制器

### 使用示例

```python
# 1. 贝叶斯优化
from core.bayesian_optimization import BayesianOptimizer

optimizer = BayesianOptimizer(parameter_bounds={
    'strength_beta': (5.0, 15.0),
    'clash_k': (3.0, 7.0)
})
optimal_params = optimizer.optimize(objective_func, n_iterations=50)

# 2. 敏感度分析
from core.bayesian_optimization import HyperparameterSensitivityAnalyzer

analyzer = HyperparameterSensitivityAnalyzer(base_params)
results = analyzer.analyze_all(objective_func, parameter_ranges)

# 3. 对比学习 RLHF
from core.contrastive_rlhf import ContrastiveRLHFTrainer

trainer = ContrastiveRLHFTrainer(reward_model)
pairs = trainer.generate_contrastive_pairs(case_data, engine_a, engine_b, years)
reward_model.train(pairs, n_epochs=100)

# 4. Transformer 位置编码调优
from core.transformer_position_tuning import PositionalEncodingTuner

tuner = PositionalEncodingTuner()
optimal_params = tuner.tune_for_long_range_dependency(timeline_data, objective)

# 5. GAT 路径过滤
from core.gat_path_filter import GATPathFilter

filter = GATPathFilter(threshold=0.1)
filtered_weights = filter.filter_paths(attention_weights, energy_paths)
```

---

## 案例：Jason D 1999年误差修正

### 问题描述

- **真实值**: 50.0
- **预测值**: -30.0
- **误差**: 80.0
- **目标**: 通过贝叶斯优化调整非线性参数，使预测更准确

### 优化脚本

```bash
# 运行贝叶斯优化
python3 scripts/bayesian_hyperparameter_tuning_jason_d_1999.py \
    --iterations 50 \
    --sensitivity \
    --output reports/jason_d_1999_optimization.json
```

### 优化参数

```json
{
  "parameter_bounds": {
    "strength_beta": [5.0, 15.0],
    "clash_k": [3.0, 7.0],
    "trine_boost": [0.1, 0.5],
    "tunneling_factor": [0.05, 0.2]
  }
}
```

### 优化流程

1. **初始随机采样** (10 个样本)
   - 探索参数空间
   - 建立初始代理模型

2. **贝叶斯优化迭代** (50 次)
   - 使用期望改进选择下一个采样点
   - 更新高斯过程模型
   - 逐步收敛到最优参数

3. **敏感度分析** (可选)
   - 分析各参数的敏感度
   - 识别关键参数
   - 绘制敏感度曲线

4. **结果验证**
   - 使用最优参数重新预测
   - 计算最终误差
   - 生成优化报告

### 预期结果

- **优化前**: 误差 80.0
- **优化后**: 误差 < 20.0（目标）
- **参数调整**: 
  - `strength_beta`: 可能降低（使能量释放更平滑）
  - `clash_k`: 可能调整（影响冲的强度因子）
  - `trine_boost`: 可能微调（三刑效应）

---

## 数学公式完整版

### 贝叶斯优化

**高斯过程**:
```
f(x) ~ GP(μ(x), k(x, x'))
μ(x) = k(x, X)ᵀ K⁻¹ y
σ²(x) = k(x, x) - k(x, X)ᵀ K⁻¹ k(X, x)
```

**期望改进**:
```
EI(x) = σ(x) [z Φ(z) + φ(z)]
z = (f_min - μ(x)) / σ(x)
```

### 对比学习

**Bradley-Terry 模型**:
```
P(A > B) = exp(r_A) / (exp(r_A) + exp(r_B))
r_A = W · encode(path_A)
```

**对比损失**:
```
L = -log(P(preferred_path))
```

### 路径过滤

**路径强度**:
```
strength(i→j) = α_ij × E_ij
```

**熵控制**:
```
H = -Σ α_ij log(α_ij)
```

---

## 最佳实践

### 1. 参数空间设计

- ✅ 使用合理的参数边界（基于物理意义）
- ✅ 避免过大的搜索空间
- ✅ 考虑参数之间的相关性

### 2. 目标函数设计

- ✅ 使用平方误差或绝对误差
- ✅ 考虑不确定性（加权）
- ✅ 加入正则化项（防止过拟合）

### 3. 迭代策略

- ✅ 初始采样数量：10-20
- ✅ 优化迭代次数：50-100
- ✅ 定期验证优化结果

### 4. 多目标优化

- ✅ 准确性（主要目标）
- ✅ 不确定性（次要目标）
- ✅ 路径合理性（约束条件）

---

## 性能指标

### 优化效率

- **单次优化时间**: < 5 分钟（50 次迭代）
- **参数空间维度**: 4-8 维
- **收敛速度**: 通常 20-30 次迭代即可收敛

### 优化效果

- **Jason D 1999 年**: 目标误差 < 20.0
- **整体命中率提升**: 待统计
- **参数稳定性**: 优化后的参数应在合理范围内

---

## 参考文档

- [V10.0 算法总纲](./V10_ALGORITHM_CONSTITUTION.md)
- [V10.0 完整技术规范](./V10_COMPLETE_TECHNICAL_SPEC.md)
- [V10.0 Jason D 推演报告](./V10_JASON_D_2015_ENERGY_BURST_ANALYSIS.md)

---

**文档维护**: Bazi Predict Team  
**最后更新**: 2025-12-17  
**状态**: ✅ 正式发布

