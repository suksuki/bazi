# 🏛️ QGA 正向拟合与建模规范 (FDS-V1.1 最终完整版)
—— 从微观波动到宏观张量的全息映射体系 ——

## 一、建模核心哲学 (The Soul)

**物理本质**：格局不是僵化的文字标签，而是八字微观场（干支粒子振动）在高维空间投射出的宏观吸引子（Attractor）。

**映射逻辑**：微观概率波（相位/振幅） $\xrightarrow{映射矩阵}$ 宏观命运张量（维度/指向）。

**主导权声明**：AI 首席分析师（Gemini）负责定义物理意象与初始权重，Cursor 负责基于此权重进行数学运算与海选。**严禁 Cursor 擅自更改物理定义的优先级**。

---

## 二、五维命运张量定义 ($\mathcal{T}_{fate}$)

所有拟合必须输出以下 5 个维度的投影值。**SAI (系统对齐指数) 为总模长**。

| 维度轴 (Axis) | 符号 | 物理定义 (Physics) | 命理意象 (Metaphysics) |
| --- | --- | --- | --- |
| **能级轴** | $\mathbf{E}$ | 系统总振幅/储能 | 寿夭、根基、抗压底气、生命长度 |
| **秩序轴** | $\mathbf{O}$ | 能量的收束与聚焦 | 权力、名誉、社会地位、贵气 |
| **物质轴** | $\mathbf{M}$ | 能量的实体转化率 | 财富、资产、执行力、现实资源 |
| **应力轴** | $\mathbf{S}$ | 内部剪切力/摩擦 | 疾厄、意外、情绪内耗、结构断裂风险 |
| **关联轴** | $\mathbf{R}$ | 场能相干性 | 六亲、情感、人际网络耦合度 |

---

## 三、六步拟合标准化工作流 (The Standard Workflow)

### Step 1: 物理意象挖掘与算子注册 (Analyst Action)

**执行者**：AI 首席分析师 (Gemini)

**核心动作**：
1. **意象解构**：将古籍判词翻译为力学状态（如：羊刃=高频震荡，七杀=高压约束）。
2. **权重定义 ($\mathbf{W}$)**：直接输出该格局在 5 轴上的初始分配比例。
3. **数学约束 (融合 Cursor 反馈)**：
   - **归一化约束**：输出的权重必须满足单位向量原则 $\sum|w_i| = 1$，防止分值膨胀。
   - **输出示例**：`{"E": 0.3, "O": 0.5, "M": 0.1, "S": 0.05, "R": 0.05}`

---

### Step 2: 全量海选与分层提纯 (Data Stratification)

**执行者**：Cursor (数据引擎)

**核心逻辑**：放弃随机截取，采用**"全量扫描 + 纯度排序 + 奇点隔离"**三步法。

**操作流程**：

1. **全量扫描 (Full Scan)**：
   - 必须遍历 51.84 万样本池，建立初始候选集 `Candidates`。
   - 按照古典标签（如"月令羊刃且透杀"）进行基础筛选。

2. **奇点捕获 (Tier X - Singularity)**：
   - **定义**：不符合常理的极端样本（如：地支三刃、七杀攻身无制）。
   - **动作**：不计入标准集，直接隔离存入 `Singularity_Set`，并注册为子专题变体（如 [A-03-X]），用于发现新物理定律。

3. **纯度排序 (Tier A - Standard)**：
   - **打分算法 ($Score_{purity}$)**：
     - **加分**：格局核心字有根 (+20)、得令 (+10)、通关有力 (+10)。
     - **减分**：混杂 (-15)、刑冲破坏 (-10)、党杀/耗能 (-15)。
   - **优选**：按分数从高到低排序，仅截取前 500 名作为标准建模的"教科书样本"。

---

### Step 3: 多维特征提取与锚点锁定 (Feature Extraction & Anchor Locking)

**执行者**：Cursor (Math Engine)

**核心动作**：
1. **张量转化**：将500个Tier A样本转化为5维向量（E, O, M, S, R）。
2. **质心计算 (Centroid Calculation)**：
   - **算法**：$\mathbf{T}_{ref} = \frac{1}{N} \sum \vec{v}_i$
   - **目的**：计算出该格局在5D空间中的几何中心，即**"标准特征锚点" (Standard Anchor)**
   - **归一化**：质心计算后必须强制归一化，确保 $\sum_{i=1}^{5} v_i = 1.0$
3. **奇点锁定**：对Tier X样本重复上述步骤，计算出**"奇点特征锚点" (Singularity Anchors)**
4. **非线性激活**：引入Sigmoid激活函数，模拟阈值效应
5. **相变判定**：定义"量子隧穿"和"结构坍缩"的临界点

---

### Step 4: 动态扩展与张量耦合 (Dynamic Simulation)

**执行者**：AI 设计逻辑 + Cursor 编写算子

**核心动作**：
1. **大运定义**：作为背景场张量，改变原局的时空曲率（长期趋势）。
2. **流年定义**：作为脉冲张量，提供瞬时击发能量。
3. **运算逻辑**：采用 **张量积 ($\otimes$)** 而非简单加法。
   - **公式**：$\mathcal{T}_{final} = \mathcal{T}_{base} \otimes \omega_{luck} \oplus \Delta \mathcal{T}_{year}$

---

### Step 5: 专题封卷与全息注册 (Registry)

**执行者**：AI 首席分析师

**核心动作**：
1. 将 Step 1 的意象、Step 3 的**"特征锚点"**和 Step 4 的"演化逻辑"，统一打包。
2. 严格按照 **附录 B (Schema V2.0)** 的格式，生成 JSON 文件存入 QGA-HR。
3. **基准锁定**：记录此时的 SAI 均值，作为后续调优的锚点。
4. **锚点注册**：将计算出的标准质心和奇点质心存入 `feature_anchors` 模块。

---

### Step 6: 动态格局识别协议 (Dynamic Pattern Recognition)

**执行者**：Core Engine (Pattern Runner)

**核心逻辑**：基于空间相似度的自动吸附机制。

**判定算法**：
1. **计算相似度**：计算当前八字张量 ($\mathbf{T}_{curr}$) 与注册表中锚点 ($\mathbf{T}_{anchor}$) 的余弦相似度
   - $\mathbf{T}_{curr}$：**原局基态 (Natal Base State)** 的5维归一化向量（格局识别只关心静态架构）
   - $\mathbf{T}_{anchor}$：从Registry加载的质心向量（`standard_centroid` 或 `singularity_centroids`）
2. **判定标准**：
   - **成格 (In-Pattern)**：相似度 > `match_threshold` (默认 0.85)
   - **破格 (Broken)**：相似度 < 0.60
   - **变异 (Mutation)**：与某奇点锚点相似度 > 0.90（触发Tier X算法）
   - **共振态 (Resonance)**：相似度 > `perfect_threshold` (默认 0.92)，触发UI高亮和`resonance_bonus`

**物理意义**：
- 夹角越小，说明八字的能量结构与"完美模型"越共振
- 大运流年的干扰属于"相位偏移"，不改变原始格局分类，只改变其吉凶响应

**实现要求**：
- 使用 `core.math_engine.calculate_cosine_similarity` 计算余弦相似度
- 如果定义了 `singularity_centroids`，需遍历计算与每个奇点的距离，取最近者
- 支持实时识别和批量识别

---

### Step 7: 动态演化协议 (Dynamic Evolution Protocol) 🌌

**执行者**：AI 架构师 + Cursor (可视化引擎)

**核心概念**：从"静态全息"到"动态流形"——我们不只要看一张图，我们要看的是**"形变 (Deformation)"**。

#### 6.1 三态演化模型

**第一态：原局基态 (The Base State)**
- **输入**：八字原局（格局已注册）
- **可视化**：生成初始的 5D 全息体（如：高耸、炽热、封闭的红色陀螺）
- **物理意义**：命主的"出厂设置"，代表在真空环境下的潜力
- **输出**：$\mathcal{T}_{base} = \{E_0, O_0, M_0, S_0, R_0\}$

**第二态：环境场加载 (Field Loading)**
- **注入大运 ($\omega_{luck}$)**：背景引力波
  - **方法**：`tensor_field_overlay`
  - **效果**：长期且缓慢改变 E, O, M 轴的基准值
  - **公式**：$\mathcal{T}_{luck} = \mathcal{T}_{base} \otimes \omega_{luck}$
  - **可视化**：持续的、缓慢的力，把整个"陀螺"向某个方向拉伸或挤压
  - **例**：走印运（生身），红色陀螺表面覆盖蓝色冷却膜，体积膨胀（能量变强）

- **注入流年 ($\Delta \mathcal{T}_{year}$)**：瞬时脉冲
  - **方法**：`impulse_impact`
  - **效果**：瞬间改变 S (应力) 轴，若超过阈值则触发断裂
  - **公式**：$\mathcal{T}_{year} = \mathcal{T}_{luck} \oplus \Delta \mathcal{T}_{year}$
  - **可视化**：一颗飞来的子弹，击中全息体的特定部位
  - **例**：流年冲刃，白色高能粒子击中陀螺核心

- **注入地理 ($\delta_{geo}$)**：环境介质密度
  - **方法**：`coefficient_multiplier`
  - **效果**：全局修正五行能量权重（如：North → Water × 1.2）
  - **公式**：$\mathcal{T}_{final} = \mathcal{T}_{year} \times (1 \pm \delta_{geo})$
  - **可视化**：改变空间的"粘稠度"或"磁偏角"
  - **例**：在南方（火地），环境变为红色背景，火属性羊刃旋转更狂暴

**第三态：演化结果 (The Resultant State)**
- **弹性形变 (Elastic Deformation)**：受到冲击，形状变扁，但流年过去后恢复原状（吉/平）
- **塑性形变 (Plastic Deformation)**：结构被永久改变（如多出一块或缺一角）（重大人生转折）
- **结构断裂 (Structural Fracture)**：全息体无法承受应力，崩解为碎片（死亡/破产）

#### 6.2 输出状态定义

| 状态 | 符号 | 判定条件 | 物理意义 | 命理意义 |
|------|------|---------|---------|---------|
| **稳定态** | `STABLE` | $S < 0.6 \times S_{threshold}$ | 系统处于平衡态 | 平顺年份 |
| **临界态** | `CRITICAL` | $0.6 \times S_{threshold} \leq S < S_{threshold}$ | 系统接近临界点 | 高风险年份 |
| **断裂态** | `FRACTURED` | $S \geq S_{threshold}$ | 系统结构崩解 | 大凶/死亡 |
| **变异态** | `MUTATED` | 触发 Tier X 奇点条件 | 系统发生相变 | 格局变异 |

#### 6.3 动态演化协议 JSON 结构

```json
"dynamic_evolution_protocol": {
  "description": "定义注入外部因子后的张量叠加规则",
  
  "luck_injection": {
    "method": "tensor_field_overlay",
    "description": "大运作为背景场张量，改变原局的时空曲率",
    "effect": "长期且缓慢改变 E, O, M 轴的基准值",
    "formula": "T_luck = T_base ⊗ ω_luck",
    "visualization": "持续的、缓慢的力，拉伸或挤压全息体"
  },
  
  "year_injection": {
    "method": "impulse_impact",
    "description": "流年作为脉冲张量，提供瞬时击发能量",
    "effect": "瞬间改变 S (应力) 轴，若超过阈值则触发断裂",
    "formula": "T_year = T_luck ⊕ ΔT_year",
    "visualization": "一颗飞来的子弹，击中全息体的特定部位"
  },
  
  "geo_correction": {
    "method": "coefficient_multiplier",
    "description": "地理环境修正五行能量权重",
    "effect": "全局修正五行能量权重 (e.g., North -> Water * 1.2)",
    "formula": "T_final = T_year × (1 ± δ_geo)",
    "visualization": "改变空间的粘稠度或磁偏角"
  },
  
  "output_states": {
    "STABLE": {
      "condition": "S < 0.6 × S_threshold",
      "description": "系统处于平衡态",
      "visualization": "全息体保持原状或轻微形变"
    },
    "CRITICAL": {
      "condition": "0.6 × S_threshold ≤ S < S_threshold",
      "description": "系统接近临界点",
      "visualization": "全息体出现明显形变，颜色变红"
    },
    "FRACTURED": {
      "condition": "S ≥ S_threshold",
      "description": "系统结构崩解",
      "visualization": "全息体崩解为碎片"
    },
    "MUTATED": {
      "condition": "触发 Tier X 奇点条件",
      "description": "系统发生相变",
      "visualization": "全息体形态突变，出现新的结构特征"
    }
  },
  
  "visualization_config": {
    "base_state_color": "根据格局五行属性（如：火→红色）",
    "luck_overlay_color": "根据大运五行属性（如：水→蓝色）",
    "year_impact_color": "根据流年五行属性（如：金→白色）",
    "geo_background": "根据地理五行偏向（如：南方→红色背景）",
    "deformation_animation": "支持时间轴回放，展示形变过程"
  }
}
```

#### 6.4 可视化交互设计：QGA 仿真控制台

**控制台逻辑**：

1. **中央视窗**：悬浮着命主的 **5D 全息体**
2. **左侧面板 (原局)**：显示基态参数（E, O, M, S, R）
3. **底部滑块 (时间)**：
   - 拖动到 **2024 (甲辰)** → 全息体微微震动，土气加重
   - 拖动到 **2026 (丙午)** → 全息体突然变红，S 轴飙升（羊刃复吟）
4. **右侧旋钮 (地理)**：
   - 旋转到 **"北京 (北)"** → 水气注入，火炎土燥的局得到缓解，全息体变得圆润
   - 旋转到 **"深圳 (南)"** → 火气注入，全息体核心温度报警

**实现要求**：
- 支持时间轴滑块（1900-2100年）
- 实时计算并可视化张量变化
- 支持地理环境切换
- 支持动画回放（形变过程）

---

## 四、稳定性与进化协议 (Stability & Evolution)

### 残差报警机制

当模型预测的"应力断裂点"与现实（或古典大凶年份）偏差超过 20% 时，触发 `CRITICAL_MISMATCH`，强制要求人工介入修正权重。

### 双重验证标准

1. **历史回测**：必须在 10,000 例名造库中跑通，验证大事件吻合率。
2. **物理自洽**：系统总能量必须守恒，不能凭空产生或消失（需符合能量守恒定律）。

### 调优红线

1. **微调幅度**：单次参数修正不得超过当前值的 15%，防止模型过拟合（Overfitting）。
2. **先拟合后审计**：必须先走完 Step 1-5 建立"尺子"，才能去"量"具体的命盘。

---

## 五、执行红线

1. **拒绝线性思维**：严禁简单的加减法，必须注册多维轴间的干涉与耦合逻辑。
2. **数据主权**：所有模型必须在全量 51.84 万样本池中验证鲁棒性。
3. **物理先行**：严禁在未定义物理张量结构前直接进行命理推断。
4. **主导权声明**：严禁 Cursor 擅自更改 AI 分析师定义的物理优先级。

---

## 附录 A：奇点判定法则 (Singularity Validation Protocol - SVP)

任何样本若要注册为 **Tier X 独立子格局**，必须同时通过以下三项物理审查：

### 1. 极值法则 (Law of Extremum)

**定义**：该样本的关键物理参数（如能级 $\mathbf{E}$ 或应力 $\mathbf{S}$）必须偏离标准分布均值 $3\sigma$ (3倍标准差) 以上。

**门槛**：处于全量样本的 Top/Bottom 0.1% 区间。

**例**：标准 A-03 的羊刃能量为 1.0~2.0；若某样本达到 4.0，即符合极值。

### 2. 相变法则 (Law of Phase Change)

**定义**：样本的物理属性不再遵循原有的线性渐变，而是发生了态的突变。

**判据**：
- **量变 $\rightarrow$ 质变**：如从"弹性形变"（抗压）转变为"脆性断裂"（破碎）。
- **能量态跃迁**：如从"受控气态"转变为"等离子体态"（无法被常规磁场约束）。

### 3. 算法失效法则 (Law of Algorithm Failure)

**定义**：使用标准格局（Tier A）的拟合方程进行计算，结果与事实（或物理逻辑）完全背离。

**判据**：
- **逻辑反转**：标准算法判定为"吉"，但 Tier X 样本表现为"凶"（或反之）。
- **例**：A-03 标准算法认为"七杀有根"是贵气（+Score）；但在 [A-03-X2] 中，七杀有根是催命符（-Score）。此时必须启用独立算法。

---

## 附录 B：全息注册表标准 JSON 架构 (QGA-HR Schema V2.0)

**—— 100% 算法复原的通用数据协议 ——**

> **重要说明**：
> 1. 本Schema定义的是**格局对象（pattern object）**的结构，不是注册表（registry）的顶层结构。完整注册表结构请参考 QGA-HR V1.0 规范。
> 2. **V2.0 不兼容 V1.0**：所有Tier A/B级数据和旧版Pattern Registry必须迁移到V2.0结构。

所有新格局的封卷注册，必须严格遵循以下 JSON 结构。任何字段的缺失都将导致 `Core.RegistryLoader` 加载失败。

### B.1 标准 Schema 定义

```json
{
  "id": "String (e.g., 'A-03')",
  "name": "String (e.g., '羊刃架杀')",
  "version": "String (e.g., '2.0')",
  "semantic_seed": {
    "physical_image": "String (e.g., 'Controlled Fusion')",
    "description": "String",
    "source": "String (e.g., 'ai_analyst_gemini')"
  },
  
  "feature_anchors": {
    "description": "基于大数据拟合算出的空间质心坐标 (The DNA of Fate)",
    
    "standard_centroid": {
      "description": "标准恒星锚点 (Tier A Mean)",
      "vector": {
        "E": "Float (0.0-1.0, 必须归一化)",
        "O": "Float (0.0-1.0, 必须归一化)",
        "M": "Float (0.0-1.0, 必须归一化)",
        "S": "Float (0.0-1.0, 必须归一化)",
        "R": "Float (0.0-1.0, 必须归一化)"
      },
      "match_threshold": 0.80,
      "perfect_threshold": 0.92
    },
    
    "singularity_centroids": [
      {
        "sub_id": "String (e.g., 'A-03-X1')",
        "description": "聚变临界型 (Tier X1 Mean)",
        "vector": {
          "E": "Float (0.0-1.0, 必须归一化)",
          "O": "Float (0.0-1.0, 必须归一化)",
          "M": "Float (0.0-1.0, 必须归一化)",
          "S": "Float (0.0-1.0, 必须归一化)",
          "R": "Float (0.0-1.0, 必须归一化)"
        },
        "match_threshold": 0.90,
        "risk_level": "CRITICAL",
        "special_instruction": "String (e.g., 'Enable Vent Logic (Disable Balance Check)')"
      }
    ]
  },
  
  "tensor_operator": {
    "weights": {
      "description": "基于 Step 3 拟合的 5 维初始权重 (Normalized)",
      "E": "Float (0.0-1.0)",
      "O": "Float (0.0-1.0)",
      "M": "Float (0.0-1.0)",
      "S": "Float (0.0-1.0)",
      "R": "Float (0.0-1.0)"
    },
    "normalized": "Boolean (必须为true)",
    "core_equation": "String (e.g., 'S_balance = E_blade / E_kill')",
    "activation_function": {
      "type": "String (e.g., 'sigmoid')",
      "parameters": {
        "collapse_threshold": "Float",
        "phase_change_point": "String"
      }
    },
    "flow_factor": {
      "formula": "String (e.g., 'S_risk = S_base / (1 + E_seal)')",
      "description": "String"
    },
    "algorithm_implementation": {
      "description": "映射到 Core 引擎的具体函数路径 (Do Not Modify)",
      "energy_calculation": {
        "function": "core.physics_engine.compute_energy_flux",
        "weights": {
          "base": "Float",
          "month_resonance": "Float",
          "rooting": "Float",
          "generation": "Float"
        }
      },
      "activation_function": {
        "function": "core.math_engine.sigmoid_variant",
        "parameters": {
          "k": "Float (e.g., 1.0)",
          "x0": "Float (e.g., 0.0)"
        }
      },
      "core_equation": {
        "function": "core.math_engine.calculate_s_balance"
      },
      "flow_factor": {
        "function": "core.math_engine.calculate_flow_factor"
      },
      "interaction_damping": {
        "function": "core.physics_engine.calculate_interaction_damping"
      },
      "phase_change": {
        "function": "core.math_engine.phase_change_determination"
      },
      "tensor_similarity": {
        "function": "core.math_engine.calculate_cosine_similarity",
        "description": "计算5维能量矢量的余弦相似度，用于Step 6格局识别"
      },
      "registry_loader": {
        "class": "core.registry_loader.RegistryLoader"
      },
      "hyper_parameters": {
        "description": "本格局专用的物理常数（可选，用于覆盖默认值）",
        "sigmoid_k": "Float (e.g., 1.0)",
        "sigmoid_x0": "Float (e.g., 0.0)",
        "base_energy_weights": {
          "blade": "Float (e.g., 1.0)",
          "kill": "Float (e.g., 0.8)",
          "print": "Float (e.g., 0.5)"
        }
      }
    }
  },

  "kinetic_evolution": {
    "trigger_operators": [
      {
        "type": "String (e.g., 'clash')",
        "description": "String"
      }
    ],
    "gain_operators": [
      {
        "type": "String (e.g., 'resonance')",
        "description": "String"
      }
    ],
    "geo_damping": "Float (e.g., 1.0)",
    "dynamic_simulation": {
      "description": "Step 4 仿真定义的流年演化系数",
      "lambda_coefficients": {
        "resonance": {
          "value": "Float (e.g., 2.5)",
          "condition": "String",
          "description": "String"
        },
        "hard_landing": {
          "value": "Float (e.g., 1.8)",
          "condition": "String",
          "description": "String"
        },
        "damping": {
          "value": "Float (e.g., 1.2)",
          "condition": "String",
          "description": "String"
        }
      },
      "fracture_threshold": "Float (e.g., 50.0)"
    }
  },

  "singularity_protocol": {
    "description": "符合奇点判定法则 (SVP) 的变体定义",
    "tier_x_count": "Integer",
    "variants": [
      {
        "id": "String (e.g., 'A-03-X1')",
        "name": "String",
        "description": "String",
        "trigger_condition": "String (Logic expression, e.g., 'blade_count >= 3')",
        "weight_override": {
          "E": "Float",
          "O": "Float",
          "M": "Float",
          "S": "Float",
          "R": "Float"
        },
        "special_instruction": "String (e.g., 'Disable balance check, enable vent logic')",
        "sample_count": "Integer"
      }
    ]
  },

  "audit_trail": {
    "sai_baseline": "Float",
    "coverage_rate": "Float",
    "hit_rate": "Float",
    "data_selection_criteria": {
      "month_lock": "String",
      "stem_reveal": "String",
      "purity_filter": {
        "exclude": ["String"]
      },
      "target_samples": "Integer"
    },
    "version_history": [
      {
        "version": "String",
        "date": "String",
        "source": "String",
        "description": "String"
      }
    ]
  }
}
```

### B.2 字段映射说明

| 规范Schema字段 | 注册表实际路径 | 说明 |
| --- | --- | --- |
| `id` | `patterns.{id}.id` | 格局唯一标识 |
| `semantic_seed.physical_image` | `patterns.{id}.semantic_seed.physical_image` | 物理原型描述 |
| `feature_anchors.standard_centroid` | `patterns.{id}.feature_anchors.standard_centroid` | **V2.0新增**：标准质心锚点 |
| `feature_anchors.singularity_centroids` | `patterns.{id}.feature_anchors.singularity_centroids` | **V2.0新增**：奇点质心锚点 |
| `tensor_operator.weights` | `patterns.{id}.tensor_operator.weights` | 五维权重 |
| `algorithm_implementation` | `patterns.{id}.tensor_operator.algorithm_implementation` | 引擎路径映射 |
| `algorithm_implementation.tensor_similarity` | `patterns.{id}.tensor_operator.algorithm_implementation.tensor_similarity` | **V2.0新增**：余弦相似度函数 |
| `kinetic_evolution.dynamic_simulation` | `patterns.{id}.kinetic_evolution.dynamic_simulation` | 动态仿真参数 |
| `singularity_protocol` | `patterns.{id}.singularity_protocol` | 奇点变体定义（V1.0兼容字段） |

### B.3 填写规范

1. **唯一性**：`id` 必须在全库唯一。
2. **可执行性**：`algorithm_implementation` 中的路径必须指向真实存在的 Python 函数。
3. **自洽性**：
   - `tensor_operator.weights` 的 E/O/M/S/R 之和必须严格等于 1.0 (归一化)
   - `feature_anchors.standard_centroid.vector` 的 E/O/M/S/R 之和必须严格等于 1.0 (归一化)
   - `feature_anchors.singularity_centroids[].vector` 的 E/O/M/S/R 之和必须严格等于 1.0 (归一化)
4. **完整性**：所有必需字段必须填写，可选字段（如 `hyper_parameters`）可根据需要添加。
5. **质心计算**：
   - `standard_centroid.vector` 必须基于 Step 3 计算的 Tier A 样本质心
   - `singularity_centroids[].vector` 必须基于 Step 3 计算的 Tier X 样本质心
   - 质心计算后必须强制归一化（使用 `tensor_normalize` 函数）
6. **阈值设置**：
   - `match_threshold`：准入门槛，默认 0.80（相似度 > 0.80 判定为"成格"）
   - `perfect_threshold`：共振态阈值，默认 0.92（相似度 > 0.92 触发UI高亮和resonance_bonus）
   - 奇点锚点的 `match_threshold` 应更严格（建议 0.90）
7. **special_instruction**：
   - 字符串格式，映射到 Physics Modifier Flags
   - 示例："Enable Vent Logic" → 强制关闭"身弱不能泄"的保护机制
   - 示例："Disable Balance Check" → 禁用平衡度检查

### B.4 必需字段清单

**核心必需字段**（缺失将导致加载失败）：
- `id`, `name`, `version` (必须为 "2.0" 或更高)
- `semantic_seed.physical_image`
- **V2.0新增**：`feature_anchors.standard_centroid.vector` (E, O, M, S, R)
- **V2.0新增**：`feature_anchors.standard_centroid.match_threshold`
- **V2.0新增**：`feature_anchors.standard_centroid.perfect_threshold`
- `tensor_operator.weights` (E, O, M, S, R)
- `tensor_operator.algorithm_implementation` (至少包含 `energy_calculation`, `activation_function`, `core_equation`)
- **V2.0新增**：`tensor_operator.algorithm_implementation.tensor_similarity` (指向 `core.math_engine.calculate_cosine_similarity`)
- `kinetic_evolution.dynamic_simulation.lambda_coefficients`
- `kinetic_evolution.dynamic_simulation.fracture_threshold`

**可选字段**（根据格局特性添加）：
- `feature_anchors.singularity_centroids[]` (仅当存在Tier X变体时)
- `tensor_operator.algorithm_implementation.hyper_parameters` (用于覆盖默认物理常数)

**可选字段**（建议填写）：
- `algorithm_implementation.hyper_parameters`
- `singularity_protocol.variants[].trigger_condition`
- `singularity_protocol.variants[].weight_override`
- `singularity_protocol.variants[].special_instruction`

---

---

## 附录 C：动态演化协议实现指南

### C.1 核心算法实现

**大运注入算法**：
```python
def apply_luck_injection(base_tensor, luck_pillar, day_master):
    """
    应用大运背景场
    
    Args:
        base_tensor: 原局基态张量 {E, O, M, S, R}
        luck_pillar: 大运干支
        day_master: 日主
        
    Returns:
        注入大运后的张量
    """
    # 计算大运对五行的影响
    luck_effect = calculate_luck_effect(luck_pillar, day_master)
    
    # 张量积运算（改变基准值）
    luck_tensor = {
        'E': base_tensor['E'] * luck_effect.get('E', 1.0),
        'O': base_tensor['O'] * luck_effect.get('O', 1.0),
        'M': base_tensor['M'] * luck_effect.get('M', 1.0),
        'S': base_tensor['S'] * luck_effect.get('S', 1.0),
        'R': base_tensor['R'] * luck_effect.get('R', 1.0)
    }
    
    return luck_tensor
```

**流年注入算法**：
```python
def apply_year_injection(luck_tensor, year_pillar, event_type='normal'):
    """
    应用流年脉冲
    
    Args:
        luck_tensor: 大运注入后的张量
        year_pillar: 流年干支
        event_type: 事件类型（'normal', 'clash', 'combination'等）
        
    Returns:
        注入流年后的张量
    """
    # 计算流年脉冲强度
    year_impulse = calculate_year_impulse(year_pillar, event_type)
    
    # 直和运算（瞬时叠加，主要影响S轴）
    year_tensor = {
        'E': luck_tensor['E'],
        'O': luck_tensor['O'],
        'M': luck_tensor['M'],
        'S': luck_tensor['S'] + year_impulse['S'],  # 应力轴叠加
        'R': luck_tensor['R']
    }
    
    return year_tensor
```

**地理修正算法**：
```python
def apply_geo_correction(year_tensor, geo_factor, geo_element):
    """
    应用地理环境修正
    
    Args:
        year_tensor: 流年注入后的张量
        geo_factor: 地理修正系数（如：1.15）
        geo_element: 地理五行偏向（如：'Fire'）
        
    Returns:
        地理修正后的最终张量
    """
    # 根据地理五行偏向，修正对应轴的权重
    element_axis_map = {
        'Fire': 'E',      # 火→能级轴
        'Earth': 'M',     # 土→物质轴
        'Metal': 'O',     # 金→秩序轴
        'Water': 'R',     # 水→关联轴
        'Wood': 'S'       # 木→应力轴
    }
    
    target_axis = element_axis_map.get(geo_element, 'E')
    
    # 系数乘法（全局修正）
    final_tensor = year_tensor.copy()
    final_tensor[target_axis] *= geo_factor
    
    return final_tensor
```

### C.2 状态判定算法

```python
def determine_output_state(final_tensor, fracture_threshold):
    """
    判定演化结果状态
    
    Args:
        final_tensor: 最终张量
        fracture_threshold: 断裂阈值
        
    Returns:
        状态字符串：'STABLE', 'CRITICAL', 'FRACTURED', 'MUTATED'
    """
    s_value = final_tensor['S']
    s_threshold = fracture_threshold
    
    if s_value < 0.6 * s_threshold:
        return 'STABLE'
    elif s_value < s_threshold:
        return 'CRITICAL'
    elif s_value >= s_threshold:
        return 'FRACTURED'
    else:
        # 检查是否触发 Tier X 奇点
        if check_tier_x_condition(final_tensor):
            return 'MUTATED'
        return 'STABLE'
```

### C.3 可视化数据格式

```json
{
  "base_state": {
    "tensor": {"E": 0.3, "O": 0.4, "M": 0.1, "S": 0.15, "R": 0.05},
    "color": "#FF0000",
    "shape": "torus",
    "size": 1.0
  },
  "luck_state": {
    "tensor": {"E": 0.35, "O": 0.45, "M": 0.12, "S": 0.15, "R": 0.05},
    "overlay_color": "#0000FF",
    "deformation": "expansion"
  },
  "year_state": {
    "tensor": {"E": 0.35, "O": 0.45, "M": 0.12, "S": 0.25, "R": 0.05},
    "impact_point": {"axis": "S", "intensity": 0.1},
    "impact_color": "#FFFFFF"
  },
  "final_state": {
    "tensor": {"E": 0.40, "O": 0.45, "M": 0.12, "S": 0.25, "R": 0.05},
    "status": "CRITICAL",
    "visualization": {
      "color": "#FF6600",
      "shape": "deformed_torus",
      "warning": true
    }
  }
}
```

---

**版本**：V1.1 最终完整版（含动态演化协议）  
**创建日期**：2025-12-29  
**更新日期**：2025-12-29  
**状态**：已批准，可直接执行  
**新增模块**：Step 6 动态演化协议 + 附录 C 实现指南
