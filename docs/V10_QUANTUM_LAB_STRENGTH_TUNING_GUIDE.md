# V10.0 量子验证页面旺衰判定基础参数调优指南

**日期**: 2025-01-17  
**版本**: V10.0  
**定位**: 第一层物理验证（旺衰判定）  
**架构**: MVC架构（View: `quantum_lab.py`, Controller: `QuantumLabController`）

---

## 📋 快速导航

- **[MVC架构文档](./V10_QUANTUM_LAB_MVC_ARCHITECTURE.md)** - 了解页面架构和代码组织
- **[调优指南](#)** - 本文档（参数调优方法和流程）

---

## 🏆 核心分析师 Final Review 摘要

**总体评价**：这是一份教科书级的工程文档，完全理解了"第一层验证（旺衰）"与"第二层观测（财富）"的物理边界。

**亮点确认**：
- ✅ 边界清洗极其彻底，完全移除了财富相关参数
- ✅ 物理模型的数学化落地，Sigmoid概率波函数定义清晰
- ✅ 回归检查的"降维打击"，只关注标签一致性，不被财富误差干扰

**微调建议（已纳入本文档）**：
1. ✅ 增加了"极弱/从格"的特殊处理说明（Step 8）
2. ✅ 可视化图表的"锚点"增强说明（Tier A标准案例参考点）
3. ✅ 调优步长的警示说明（防止过度调整）

---

## 📋 执行摘要

本文档详细说明量子验证页面（`quantum_lab.py`）中通过调整**基础参数**来优化**旺衰判定**的思路、模块、算法和自动化调优方案。

### 核心定位

**量子验证页面（第一层验证）**：
- ✅ **目标**：只判定旺衰（身强身弱）
- ✅ **输入**：八字、大运、流年、地域（通过MCP上下文注入）
- ✅ **输出**：身强/身弱/平衡/从格标签 + 身强分数
- ❌ **不涉及**：财富、事业、感情等宏观观测（这些在第二层 `wealth_verification.py`）

### MVC架构

**View层** (`ui/pages/quantum_lab.py`):
- UI展示和用户交互
- 参数输入界面
- 结果可视化
- 只调用Controller，不直接调用Engine

**Controller层** (`controllers/quantum_lab_controller.py`):
- 封装所有算法逻辑
- 协调Engine和Model的调用
- 处理MCP上下文注入
- 提供统一的数据接口给View层

**Engine/Model层**:
- 核心计算引擎（`core/engine_graph.py`）
- 数据模型（`core/bazi_profile.py`）

> 📖 详细架构说明请参考：[V10.0 量子验证页面 MVC 架构文档](./V10_QUANTUM_LAB_MVC_ARCHITECTURE.md)

---

## 🎯 一、调优思路

### 1.1 核心调优原则

#### 1.1.1 物理模型优先
所有参数调整必须符合物理模型：
- **能量守恒**：日主能量 = Self_Team / Total_Energy
- **相变原理**：身强身弱的临界点是能量占比的相变点
- **注意力机制**：GAT动态调整不同因素的权重

#### 1.1.2 基础参数分层

**第一层参数**（直接影响旺衰判定）：
1. **旺衰概率场参数**（V10.0新增）
   - `energy_threshold_center`：能量阈值中心点（相变临界点）
   - `phase_transition_width`：相变宽度（Sigmoid曲线陡峭度）
   - `attention_dropout`：GAT注意力稀疏度（噪声过滤）

2. **基础物理参数**（影响能量计算）
   - 宫位引力（Pillar Weights）：年、月、日、时柱权重
   - 季节权重（Season Weights）：月令对日主的生克影响
   - 藏干比例（Hidden Stem Ratios）：地支藏干能量分配

3. **结构参数**（影响根气和透干）
   - 通根系数（Rooting Weight）：根气强度
   - 透干加成（Exposed Boost）：天干透出加成
   - 自坐强根（Same Pillar Bonus）：同柱干支互动

4. **交互参数**（影响能量流转）
   - 天干五合（Stem Five Combination）：合化/合绊
   - 地支成局（Branch Combo）：三合/三会/半合
   - 冲的折损（Clash Damping）：冲的能量损耗

5. **流转参数**（影响能量传导）
   - 输入阻抗（Resource Impedance）：印星生身的阻力
   - 输出粘滞（Output Viscosity）：食伤泄气的阻力
   - 系统熵（System Entropy）：全局能量损耗

**第二层参数**（财富预测专用，**不在此页面调优**）：
- `seal_bonus`（印星特权加成）
- `opportunity_scaling`（机会加成缩放）
- `nonlinear_damping`（非线性阻尼）
- 财库物理（Vault Physics）

### 1.2 调优目标

**优化目标函数**：
```
Minimize: Σ |predicted_label - ground_truth_label|
```

**约束条件**：
- 所有基础参数在合理范围内
- 不破坏其他案例的判定准确性（全局回归检查）
- 保持物理模型的合理性

### 1.3 调优策略

#### 策略1：参数敏感度分析
1. **单参数扫描**：固定其他参数，扫描单个参数范围
2. **计算损失**：对每个参数值，计算所有案例的判定误差
3. **识别敏感参数**：找出对判定结果影响最大的参数

#### 策略2：多参数联合优化
1. **贝叶斯优化**：使用Gaussian Process寻找最优参数组合
2. **网格搜索**：小范围参数的网格搜索（计算量大）
3. **梯度优化**：如果损失函数可微，使用梯度下降

#### 策略3：全局回归检查
1. **基准对比**：与V9.3基线配置对比
2. **匹配率统计**：计算所有案例的判定匹配率
3. **回归检测**：识别优化后变差的案例

---

## 🔬 二、核心算法

### 2.1 旺衰判定算法流程

```python
def calculate_strength_score(day_master: str) -> Dict[str, Any]:
    """
    计算身旺分数（占比法）- 标准化的百分制评分
    
    使用公式：Strength_Score = (Self_Team / Total_Energy) * 100.0
    """
    # Step 1: 初始化节点（Phase 1）
    self.initialize_nodes(bazi, day_master, ...)
    
    # Step 2: 构建邻接矩阵（Phase 2）
    self.build_adjacency_matrix(...)
    
    # Step 3: 能量传播（Phase 3）
    H = self.propagate(max_iterations=10)
    
    # Step 4: 计算日主阵营能量
    self_team_energy = 0.0  # Self + Resource + Peer
    total_energy = 0.0
    
    for node in self.nodes:
        total_energy += node.current_energy
        if node.element == dm_element or node.element == resource_element:
            self_team_energy += node.current_energy
    
    # Step 5: 计算占比分数
    strength_score = (self_team_energy / total_energy) * 100.0
    
    # Step 6: 应用旺衰概率波函数（V10.0）
    # 使用Sigmoid函数平滑判定
    strength_normalized = strength_score / 100.0
    strength_probability = sigmoid(
        strength_normalized * 10.0,  # 归一化到0-10范围
        threshold=energy_threshold_center,
        width=phase_transition_width
    )
    
    # Step 7: 判定标签（基础三段式）
    if strength_probability > 0.6:
        strength_label = "Strong"
    elif strength_probability > 0.4:
        strength_label = "Balanced"
    else:
        strength_label = "Weak"
    
    # Step 8: [V10.0 核心分析师建议] 极弱/从格特殊处理
    # 如果 strength_probability < 0.15 且满足特定结构条件（如截脚/无根），
    # 应触发 Extreme_Weak 或 Follower 标签
    if strength_probability < 0.15:
        # 检查是否为从格（Follower）
        follower_result = self._detect_follower_grid(day_master, {
            'strength_score': strength_score,
            'self_team_energy': self_team_energy,
            'total_energy': total_energy
        })
        if follower_result:
            strength_label = follower_result.get('label', 'Follower')  # 可能是 "Follower" 或 "Special_Follow"
        elif self._has_structural_collapse_condition():  # 如 Jason E 的截脚结构
            strength_label = "Extreme_Weak"
        else:
            strength_label = "Weak"  # 保持 Weak，但标记为极弱
    
    return {
        'strength_score': strength_score,
        'strength_label': strength_label,
        'strength_probability': strength_probability
    }
```

### 2.2 旺衰概率波函数（Sigmoid激活）

**数学公式**：
```
P(Strong) = 1 / (1 + exp(-k * (E - E₀)))
```

其中：
- `E`：归一化的日主能量占比（0-10范围）
- `E₀`：能量阈值中心点（`energy_threshold_center`，默认2.89）
- `k`：陡峭度参数（`k = 10.0 / phase_transition_width`）

**参数影响**：
- **energy_threshold_center**：
  - 值越大 → 判定为"身强"需要更高的能量占比
  - 示例：2.89 vs 3.0，在能量占比2.95时，前者判为"身强"，后者判为"身弱"

- **phase_transition_width**：
  - 值越大 → Sigmoid曲线越平缓，过渡带越宽
  - 值越小 → 曲线越陡峭，接近硬阈值
  - 默认10.0，提供平滑过渡

### 2.3 GAT动态注意力机制

**作用**：根据八字特征动态调整不同因素的权重

**算法**：
```python
def calculate_dynamic_strength_weights(bazi_features, pattern_type):
    """
    根据八字特征计算动态权重
    
    Args:
        bazi_features: 八字特征（是否有财库、冲的数量等）
        pattern_type: 格局类型（'wealth_vault', 'seal_help', 等）
    """
    # 使用图注意力网络计算注意力权重
    attention_weights = gat_network.compute_attention(
        nodes=self.nodes,
        dropout=attention_dropout  # 过滤杂气
    )
    
    # 根据格局类型调整权重
    if pattern_type == 'wealth_vault':
        # 财库格局：增强库的影响权重
        attention_weights['vault'] *= 1.5
    elif pattern_type == 'seal_help':
        # 印星帮身：增强印星的权重
        attention_weights['resource'] *= 1.3
    
    return attention_weights
```

**参数影响**：
- **use_gat**：
  - `True`：启用动态注意力，局部隔离调优
  - `False`：使用固定权重矩阵

- **attention_dropout**：
  - 值越大 → 过滤更多"杂气"（噪声）
  - 值越小 → 保留更多细节信息
  - 默认0.29（从敏感度分析得出）

---

## 📦 三、核心模块

### 3.1 引擎模块 (`core/engine_graph.py`)

**核心方法**：
- `calculate_strength_score(day_master)`：计算旺衰分数和标签
- `initialize_nodes(bazi, day_master, ...)`：初始化节点能量
- `build_adjacency_matrix(...)`：构建邻接矩阵
- `propagate(...)`：能量传播迭代

**配置读取**：
```python
strength_config = self.config.get('strength', {})
energy_threshold_center = strength_config.get('energy_threshold_center', 2.89)
phase_transition_width = strength_config.get('phase_transition_width', 10.0)

gat_config = self.config.get('gat', {})
use_gat = gat_config.get('use_gat', True)
attention_dropout = strength_config.get('attention_dropout', gat_config.get('attention_dropout', 0.29))
```

### 3.2 配置模块 (`core/config_schema.py`)

**旺衰判定参数定义**：
```python
"strength": {
    "energy_threshold_center": 2.89,      # 最优值（从敏感度分析得出）
    "phase_transition_width": 10.0,       # 默认值，平滑过渡
    "attention_dropout": 0.29             # 最优值（从敏感度分析得出）
}

"gat": {
    "use_gat": True,                      # 是否启用GAT
    "attention_dropout": 0.29,            # GAT注意力稀疏度
    # ... 其他GAT参数
}
```

### 3.3 UI模块 (`ui/pages/quantum_lab.py`)

**参数面板**（侧边栏）：
1. **基础场域 (Physics)**：宫位引力、季节权重
2. **粒子动态 (Structure)**：通根、透干、自坐
3. **几何交互 (Interactions)**：五合、成局、冲合
4. **能量流转 (Flow)**：输入阻抗、输出粘滞、系统熵
5. **⚛️ 旺衰概率场 (Strength Probability Field)**：
   - `energy_threshold_center` 滑块
   - `phase_transition_width` 滑块
   - `use_gat` 开关
   - `attention_dropout` 滑块
   - 实时Sigmoid曲线可视化

**应用并回测**：
- 构建完整配置
- 执行全局回归检查（使用`StrengthRegressionGuard`）
- 显示匹配率统计
- 保存配置快照

### 3.4 回归检查模块 (`ui/utils/strength_regression_guard.py`)

**功能**：
- 加载所有有`ground_truth.strength`的案例
- 计算基准配置和新配置的判定结果
- 统计匹配率、回归案例、改进案例

**API**：
```python
guard = StrengthRegressionGuard()
health_results = guard.check_global_health(
    new_config=final_full_config,
    cases_data=cases,
    engine_class=GraphNetworkEngine
)

# 结果格式
{
    'cases': {
        'CASE_ID': {
            'name': '案例名称',
            'ground_truth': 'Strong',
            'baseline_label': 'Weak',
            'new_label': 'Strong',
            'status': 'improved',  # improved/regressed/matched/mismatched
            'status_icon': '✅'
        }
    },
    'summary': {
        'total_cases': 16,
        'matched': 14,
        'mismatched': 2,
        'match_rate': 87.5,
        'overall_status': 'good'  # good/warning/critical
    }
}
```

### 3.5 可视化模块 (`ui/utils/strength_probability_visualization.py`)

**功能**：绘制旺衰概率波函数（Sigmoid曲线）

**API**：
```python
fig = plot_strength_probability_curve(
    energy_threshold_center=2.89,
    phase_transition_width=10.0,
    current_case_energy=2.95  # 可选，标记当前案例
)
```

**可视化内容**：
- Sigmoid曲线（身强概率 vs 能量占比）
- 阈值中心线（临界点）
- 当前案例能量位置标记（如果提供）
- 身弱/身强区域标注

**📊 [V10.0 核心分析师建议] 锚点增强**：
在Sigmoid曲线上，不仅标记当前案例的红点，最好能同时显示 **Tier A 标准案例**（如乾隆、梦露、乔丹等）的灰点位置。

**目的**：让用户在调优时有一个"物理参照系"。例如，"我的案例能量在梦露左边，那肯定是很弱了"。

**实现建议**：
```python
# Tier A 标准案例锚点
tier_a_anchors = [
    {'name': '乾隆皇帝', 'energy': 3.5, 'label': 'Strong'},
    {'name': '玛丽莲·梦露', 'energy': 2.1, 'label': 'Weak'},
    {'name': '迈克尔·乔丹', 'energy': 1.2, 'label': 'Follower'},
    {'name': 'Jason E', 'energy': 0.8, 'label': 'Extreme_Weak'}
]

# 在图表上添加灰点标记
for anchor in tier_a_anchors:
    fig.add_trace(go.Scatter(
        x=[anchor['energy']],
        y=[sigmoid(anchor['energy'], energy_threshold_center, phase_transition_width)],
        mode='markers',
        name=f"参考: {anchor['name']}",
        marker=dict(color='#95A5A6', size=10, symbol='circle'),
        hovertemplate=f"{anchor['name']}<br>能量: {anchor['energy']:.2f}<br>标签: {anchor['label']}<extra></extra>"
    ))
```

---

## 🤖 四、自动化调优方案

### 4.1 参数敏感度分析脚本

**脚本位置**：`scripts/strength_parameter_sensitivity_analysis.py`

**功能**：
1. **单参数扫描**：逐个参数扫描，计算损失
2. **最优值识别**：找到损失最小的参数值
3. **敏感度计算**：计算参数对损失的梯度（敏感度）
4. **报告生成**：生成JSON报告和可视化图表

**使用方法**：
```bash
python3 scripts/strength_parameter_sensitivity_analysis.py --output reports/
```

**输出**：
- `reports/strength_parameter_sensitivity_report.json`
- `reports/strength_parameter_sensitivity_curves.png`

**分析的参数**：
1. `energy_threshold_center`（1.0 - 5.0）
2. `phase_transition_width`（1.0 - 20.0）
3. `attention_dropout`（0.0 - 0.5）

### 4.2 贝叶斯优化脚本（待实现）

**设计思路**：

```python
from core.bayesian_optimization import BayesianOptimizer

class StrengthOptimizationObjective:
    """旺衰判定优化目标函数"""
    
    def __call__(self, energy_threshold_center, phase_transition_width, attention_dropout):
        """
        计算目标函数值（损失）
        
        Returns:
            损失值（越小越好）
        """
        config = {
            'strength': {
                'energy_threshold_center': energy_threshold_center,
                'phase_transition_width': phase_transition_width,
                'attention_dropout': attention_dropout
            }
        }
        
        total_error = 0.0
        for case in test_cases:
            # 计算预测标签
            predicted_label = self._calculate_strength_label(case, config)
            ground_truth = case['ground_truth']['strength']
            
            # 计算误差（0-1损失）
            if predicted_label != ground_truth:
                total_error += 1.0
        
        return total_error / len(test_cases)

# 参数范围
param_bounds = {
    'energy_threshold_center': (1.0, 5.0),
    'phase_transition_width': (1.0, 20.0),
    'attention_dropout': (0.0, 0.5)
}

# 贝叶斯优化
optimizer = BayesianOptimizer(param_bounds)
best_params = optimizer.optimize(objective, n_iterations=50)
```

**优势**：
- 智能采样：利用Gaussian Process选择最有希望的参数组合
- 高效搜索：避免网格搜索的计算量爆炸
- 自动平衡：同时优化多个参数，找到全局最优

### 4.3 UI集成自动化调优（待实现）

**设计思路**：

```python
# 在 quantum_lab.py 中添加"自动调优"按钮
if st.sidebar.button("🤖 自动调优 (Auto-Tune)", type="secondary"):
    with st.spinner("正在进行自动参数调优..."):
        # 1. 加载测试案例
        test_cases = [c for c in cases if c.get('ground_truth', {}).get('strength')]
        
        # 2. 运行贝叶斯优化
        optimizer = StrengthBayesianOptimizer(test_cases)
        best_params = optimizer.optimize(n_iterations=30)
        
        # 3. 更新UI滑块值
        st.session_state['strength_energy_threshold'] = best_params['energy_threshold_center']
        st.session_state['strength_phase_width'] = best_params['phase_transition_width']
        st.session_state['strength_attention_dropout'] = best_params['attention_dropout']
        
        # 4. 显示优化结果
        st.success(f"✅ 优化完成！匹配率: {optimizer.best_match_rate:.1f}%")
        st.json(best_params)
        
        # 5. 自动执行"应用并回测"
        # ...
```

### 4.4 持续监控和自动调整（未来规划）

**设计思路**：
1. **实时监控**：监控所有案例的判定准确率
2. **自动检测**：当准确率下降时，自动触发优化
3. **增量优化**：基于历史数据，增量调整参数
4. **A/B测试**：对比新旧参数的效果

---

## 📊 五、调优工作流

### 5.1 手动调优流程

```
1. 打开量子验证页面（quantum_lab.py）
   ↓
2. 选择测试案例（有ground_truth.strength的案例）
   ↓
3. 查看当前判定结果（身强/身弱标签）
   ↓
4. 调整参数（通过侧边栏滑块）
   - energy_threshold_center（1.0 - 5.0）
   - phase_transition_width（1.0 - 20.0）
   - attention_dropout（0.0 - 0.5）
   ↓
5. 实时查看Sigmoid曲线变化
   ↓
6. 点击"应用并回测"
   ↓
7. 查看全局回归检查结果
   - 匹配率统计
   - 改进/回归案例列表
   ↓
8. 如果满意，保存为黄金参数
```

### 5.2 自动化调优流程

```
1. 运行敏感度分析脚本
   python3 scripts/strength_parameter_sensitivity_analysis.py
   ↓
2. 查看分析报告
   - 识别最敏感的参数
   - 查看最优参数值
   ↓
3. 运行贝叶斯优化（如果已实现）
   python3 scripts/bayesian_strength_optimization.py
   ↓
4. 验证优化结果
   - 检查所有案例的匹配率
   - 确认无回归
   ↓
5. 应用到配置
   - 更新config_schema.py
   - 或保存为config/parameters.json
```

---

## 🎯 六、关键参数详解

### 6.1 energy_threshold_center（能量阈值中心点）

**默认值**：2.89（从Jason D案例敏感度分析得出）

**物理意义**：
- 定义身强身弱的"临界点"
- 能量占比 > 阈值 → 身强概率 > 50%
- 能量占比 < 阈值 → 身强概率 < 50%

**调优影响**：
- **增大**（2.89 → 3.0）：更容易判为"身弱"
  - 适用场景：案例普遍偏强，需要更严格的标准
- **减小**（2.89 → 2.5）：更容易判为"身强"
  - 适用场景：案例普遍偏弱，需要放宽标准

**调优策略**：
1. 如果大部分案例被判为"身强"（但真实是"身弱"）→ 增大阈值
2. 如果大部分案例被判为"身弱"（但真实是"身强"）→ 减小阈值

**⚠️ [V10.0 核心分析师建议] 调优步长警示**：
虽然UI滑块步长为 0.01，但应强调 **"牵一发而动全身"**。修改 `energy_threshold_center` 0.1 个单位可能导致 30% 的案例标签翻转（从Strong变为Weak，或反之）。

**推荐做法**：
- **手动调优**：每次调整不超过 0.05，并立即执行回归检查
- **自动调优**：强烈推荐使用 **贝叶斯优化** 来寻找该参数的最优值，而不是手动盲调
- **敏感性分析**：在调整前，先运行敏感度分析脚本，了解参数的影响范围

### 6.2 phase_transition_width（相变宽度）

**默认值**：10.0

**物理意义**：
- 控制Sigmoid曲线的"陡峭程度"
- 值越大 → 曲线越平缓 → 过渡带越宽
- 值越小 → 曲线越陡峭 → 接近硬阈值

**调优影响**：
- **增大**（10.0 → 15.0）：过渡带变宽，边界案例更易波动
  - 适用场景：需要更"柔和"的判定，减少硬边界
- **减小**（10.0 → 5.0）：过渡带变窄，判定更"硬"
  - 适用场景：需要更明确的判定，减少模糊地带

**调优策略**：
- 通常保持默认值10.0，除非有特殊需求
- 如果边界案例（能量占比在阈值附近）判定不稳定，可以增大

### 6.3 attention_dropout（GAT注意力稀疏度）

**默认值**：0.29（从敏感度分析得出）

**物理意义**：
- GAT注意力网络的Dropout率
- 过滤"杂气"（噪声），只保留关键信息
- 防止过拟合

**调优影响**：
- **增大**（0.29 → 0.4）：过滤更多信息，模型更"保守"
  - 适用场景：数据有噪声，需要更强的泛化能力
- **减小**（0.29 → 0.1）：保留更多信息，模型更"敏感"
  - 适用场景：需要捕捉细节特征

**调优策略**：
- 如果模型对训练案例过拟合，增大dropout
- 如果模型过于保守，无法识别关键特征，减小dropout

---

## 🔍 七、调试和诊断

### 7.1 判定错误诊断

**问题**：案例被误判（如真实是"Strong"但预测为"Weak"）

**诊断步骤**：
1. **查看能量占比**：
   ```python
   strength_result = engine.calculate_strength_score(day_master)
   strength_score = strength_result['strength_score']  # 0-100
   strength_normalized = strength_score / 100.0  # 0-1
   ```

2. **查看Sigmoid概率**：
   ```python
   strength_probability = sigmoid(
       strength_normalized * 10.0,
       threshold=energy_threshold_center,
       width=phase_transition_width
   )
   ```

3. **分析原因**：
   - 如果`strength_probability`接近0.5 → 边界案例，调整阈值
   - 如果能量占比计算有误 → 检查基础参数（physics, structure等）
   - 如果概率计算有误 → 检查Sigmoid参数

### 7.2 参数影响分析

**工具**：使用敏感度分析脚本

```bash
# 分析单个参数的敏感度
python3 scripts/strength_parameter_sensitivity_analysis.py
```

**输出解读**：
- `optimal_value`：最优参数值
- `optimal_loss`：最优损失值
- `sensitivity_range`：敏感度范围（绝对值越大，影响越大）

### 7.3 全局回归诊断

**工具**：使用`StrengthRegressionGuard`

**检查点**：
1. **匹配率**：应该 ≥ 80%（理想 ≥ 90%）
2. **回归案例**：应该为0（不允许有回归）
3. **改进案例**：越多越好

**如果出现回归**：
1. 查看回归案例的特征
2. 分析参数调整对这些案例的影响
3. 考虑使用加权优化（给回归案例更高权重）

---

## 📈 八、调优最佳实践

### 8.1 参数调整顺序

**推荐顺序**：
1. **energy_threshold_center**（最重要，直接影响判定）
2. **attention_dropout**（影响特征提取）
3. **phase_transition_width**（微调，通常保持默认）

### 8.2 调优范围建议

| 参数 | 默认值 | 建议调优范围 | 步长 |
|------|--------|------------|------|
| energy_threshold_center | 2.89 | 2.0 - 4.0 | 0.01 |
| phase_transition_width | 10.0 | 5.0 - 20.0 | 0.5 |
| attention_dropout | 0.29 | 0.1 - 0.5 | 0.01 |

### 8.3 调优检查清单

- [ ] 运行敏感度分析，识别关键参数
- [ ] 小步调整（每次调整 ≤ 10%）
- [ ] 每次调整后执行全局回归检查
- [ ] 记录每次调整的结果和原因
- [ ] 如果出现回归，立即回滚
- [ ] 最终验证所有案例的匹配率 ≥ 90%

---

## 📚 九、相关文档和资源

### 9.1 代码文件
- `ui/pages/quantum_lab.py`：量子验证页面UI
- `core/engine_graph.py`：引擎核心算法（`calculate_strength_score`）
- `core/config_schema.py`：参数配置定义
- `ui/utils/strength_regression_guard.py`：回归检查模块
- `ui/utils/strength_probability_visualization.py`：可视化模块
- `scripts/strength_parameter_sensitivity_analysis.py`：敏感度分析脚本

### 9.2 文档
- `docs/V10_QUANTUM_LAB_UI_REVIEW.md`：UI回顾和优化建议
- `docs/V10_STRENGTH_CASE_FORMAT.md`：旺衰案例格式规范
- `docs/V10_TUNING_REVIEW_SUMMARY.md`：调优回顾总结

### 9.3 测试用例
- `data/calibration_cases.json`：包含16个旺衰案例（target_focus == "STRENGTH"）

---

## ✅ 十、总结

量子验证页面的旺衰判定调优是一个**系统性的工程**，需要：

1. **理解物理模型**：能量占比、相变原理、注意力机制
2. **识别关键参数**：energy_threshold_center是最重要的参数
3. **使用工具**：敏感度分析、回归检查、可视化
4. **遵循流程**：小步调整、全局验证、记录结果
5. **保持平衡**：在准确率和稳定性之间找到平衡

通过系统性的调优，我们可以逐步提高旺衰判定的准确性，为后续的财富预测打下坚实的基础。

---

**下一步**：开始使用新导入的15个真实案例进行调优验证。

