# V10.0 旺衰判定参数调优工作流

**日期**: 2025-01-17  
**版本**: V10.0  
**状态**: ✅ 工具已就绪

---

## 📋 概述

本文档提供完整的旺衰判定参数调优工作流程，包括工具使用、调优策略和最佳实践。

---

## 🎯 调优目标

**优化目标**: 最大化旺衰判定准确率（匹配率）

```
目标函数: Max(匹配率) = (匹配案例数 / 总案例数) × 100%
约束条件: 所有参数在合理范围内
```

---

## 🛠️ 调优工具

### 1. 参数调优脚本

**文件**: `scripts/strength_parameter_tuning.py`

**功能**:
- ✅ 参数敏感度分析
- ✅ 网格搜索优化
- ✅ 回归测试
- ✅ 报告生成

---

## 📊 调优流程

### 阶段1: 基线测试

**目的**: 了解当前参数配置的性能

```bash
# 测试当前参数配置
python3 scripts/strength_parameter_tuning.py --mode test
```

**输出示例**:
```
🧪 测试当前参数配置...
✅ 加载了 15 个旺衰校准案例

✅ 当前配置性能:
   匹配率: 80.0%
   匹配案例: 12/15

📋 详细结果:
   ✅ 乾隆皇帝: Strong (GT: Strong, Score: 85.2)
   ✅ 埃隆·马斯克: Weak (GT: Weak, Score: 45.8)
   ❌ 唐纳德·特朗普: Balanced (GT: Strong, Score: 55.0)
   ...
```

---

### 阶段2: 敏感度分析

**目的**: 识别关键参数，了解参数对结果的影响

#### 2.1 分析 energy_threshold_center

这是最重要的参数，控制身强身弱的临界点。

```bash
python3 scripts/strength_parameter_tuning.py \
    --mode sensitivity \
    --param strength.energy_threshold_center \
    --min 2.0 \
    --max 4.0 \
    --steps 20
```

**分析要点**:
- 找出匹配率最高的参数值
- 观察参数变化对匹配率的影响曲线
- 识别参数敏感区域

#### 2.2 分析 phase_transition_width

控制Sigmoid曲线的陡峭度，影响过渡区的宽度。

```bash
python3 scripts/strength_parameter_tuning.py \
    --mode sensitivity \
    --param strength.phase_transition_width \
    --min 5.0 \
    --max 20.0 \
    --steps 15
```

#### 2.3 分析 GAT 参数（如果启用）

```bash
# attention_dropout
python3 scripts/strength_parameter_tuning.py \
    --mode sensitivity \
    --param gat.attention_dropout \
    --min 0.1 \
    --max 0.5 \
    --steps 10
```

---

### 阶段3: 参数优化

**目的**: 通过网格搜索找到最优参数组合

```bash
python3 scripts/strength_parameter_tuning.py --mode optimize
```

**默认优化参数**:
- `energy_threshold_center`: 2.0 ~ 4.0 (10步)
- `phase_transition_width`: 5.0 ~ 20.0 (8步)

**输出示例**:
```
🔎 开始网格搜索...
   参数数量: 2
   总组合数: 88

   [1/88] energy_threshold_center=2.000, phase_transition_width=5.000: 73.3%
   [2/88] energy_threshold_center=2.000, phase_transition_width=7.143: 76.7%
   ...

🎯 最优配置找到:
   匹配率: 93.3%

📋 最优参数:
   energy_threshold_center: 2.890
   phase_transition_width: 10.000
```

---

### 阶段4: 验证和应用

**目的**: 验证优化后的参数，应用到配置文件

#### 4.1 验证优化结果

使用最优参数测试：

```python
# 手动验证脚本示例
from controllers.quantum_lab_controller import QuantumLabController
import json

controller = QuantumLabController()

# 加载最优参数
optimal_config = {
    'strength': {
        'energy_threshold_center': 2.89,
        'phase_transition_width': 10.0
    }
}

controller.update_config(optimal_config)

# 测试所有案例
# ... 运行测试代码 ...
```

#### 4.2 应用到配置文件

将最优参数保存到 `config/parameters.json`:

```bash
# 方法1: 手动编辑 config/parameters.json
# 方法2: 使用脚本更新（需要实现）
```

---

## 📈 调优策略

### 策略1: 单参数扫描

**适用场景**: 初次调优，了解参数影响

**步骤**:
1. 选择一个参数
2. 在合理范围内扫描
3. 记录匹配率变化
4. 找出最优值

**优点**: 简单直观，易于理解

**缺点**: 无法考虑参数间的交互

---

### 策略2: 网格搜索

**适用场景**: 优化2-3个关键参数

**步骤**:
1. 选择2-3个关键参数
2. 定义每个参数的搜索范围
3. 遍历所有组合
4. 找出最优组合

**优点**: 能找到全局最优（在搜索范围内）

**缺点**: 组合数增长快，计算量大

---

### 策略3: 迭代优化

**适用场景**: 精细调优，逐步逼近最优值

**步骤**:
1. 从基线开始
2. 每次只调整一个参数
3. 如果改进，保留；否则回滚
4. 重复直到收敛

**优点**: 稳健，不易破坏已有性能

**缺点**: 可能陷入局部最优

---

## ⚠️ 注意事项

### 1. 参数范围约束

**energy_threshold_center**:
- 理论范围: 0.5 ~ 5.0
- 推荐范围: 2.0 ~ 4.0
- 默认值: 2.89

**phase_transition_width**:
- 理论范围: 1.0 ~ 50.0
- 推荐范围: 5.0 ~ 20.0
- 默认值: 10.0

### 2. 过度拟合风险

- ✅ 使用所有案例进行优化，而不是只优化单个案例
- ✅ 保留部分案例作为验证集（如果有足够案例）
- ✅ 关注全局匹配率，而不是单个案例

### 3. 回归检查

每次参数调整后，应该：
- ✅ 测试所有案例
- ✅ 确保没有案例从匹配变为不匹配
- ✅ 如果出现回归，考虑回滚或重新优化

---

## 📋 调优检查清单

### 调优前

- [ ] 确认校准案例已加载
- [ ] 记录基线性能
- [ ] 确定调优目标（匹配率目标）
- [ ] 选择调优策略

### 调优中

- [ ] 小步调整（每次调整 ≤ 10%）
- [ ] 记录每次调整的结果
- [ ] 监控是否有回归
- [ ] 如果出现回归，立即停止

### 调优后

- [ ] 验证最优参数在所有案例上的表现
- [ ] 生成调优报告
- [ ] 更新配置文件
- [ ] 在UI中验证新参数
- [ ] 记录调优结果和原因

---

## 📊 调优报告模板

```json
{
  "timestamp": "20250117_143000",
  "baseline": {
    "match_rate": 80.0,
    "matched_cases": 12,
    "total_cases": 15
  },
  "optimized": {
    "match_rate": 93.3,
    "matched_cases": 14,
    "total_cases": 15,
    "parameters": {
      "energy_threshold_center": 2.89,
      "phase_transition_width": 10.0
    }
  },
  "improvement": {
    "match_rate_delta": 13.3,
    "matched_cases_delta": 2
  },
  "case_results": [...]
}
```

---

## 🚀 快速开始示例

### 完整调优流程示例

```bash
# 1. 基线测试
python3 scripts/strength_parameter_tuning.py --mode test --output reports/baseline.json

# 2. energy_threshold_center 敏感度分析
python3 scripts/strength_parameter_tuning.py \
    --mode sensitivity \
    --param strength.energy_threshold_center \
    --min 2.5 \
    --max 3.5 \
    --steps 20

# 3. 参数优化（网格搜索）
python3 scripts/strength_parameter_tuning.py --mode optimize

# 4. 验证最优参数
# （手动验证或使用脚本）
```

---

## 📚 相关文档

- [V10.0 量子验证页面旺衰判定基础参数调优指南](./V10_QUANTUM_LAB_STRENGTH_TUNING_GUIDE.md)
- [V10.0 量子验证页面 MVC 架构文档](./V10_QUANTUM_LAB_MVC_ARCHITECTURE.md)

---

**维护者**: Bazi Predict Team  
**最后更新**: 2025-01-17

