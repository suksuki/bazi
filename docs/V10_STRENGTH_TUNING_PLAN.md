# V10.0 旺衰判定参数调优计划

**日期**: 2025-01-17  
**当前状态**: 基线测试完成（匹配率 62.5%）

---

## 📊 当前性能基线

### 测试结果

- **匹配率**: 62.5% (25/40)
- **匹配案例**: 25个
- **不匹配案例**: 15个

### 主要问题案例

1. **埃隆·马斯克**: Moderate (GT: Weak, Score: 71.6)
2. **史蒂夫·乔布斯**: Strong (GT: Weak, Score: 151.2) - 明显偏高
3. **迈克尔·乔丹**: Weak (GT: Follower, Score: 19.1) - 需要识别从格
4. **比尔·盖茨**: Weak (GT: Strong, Score: 42.7) - 明显偏低
5. **阿道夫·希特勒**: Moderate (GT: Weak, Score: 58.5)

---

## 🎯 调优目标

| 阶段 | 目标匹配率 | 说明 |
|------|-----------|------|
| **阶段1** | 75%+ | 改善明显的误判案例 |
| **阶段2** | 85%+ | 精细调优，处理边界案例 |
| **阶段3** | 90%+ | 最终优化，达到工业级精度 |

---

## 🛠️ 调优方案

### 方案1: 单参数敏感度分析（推荐先执行）

**目的**: 了解每个参数对结果的影响，找出关键参数

#### Step 1: energy_threshold_center 敏感度分析

```bash
python3 scripts/strength_parameter_tuning.py \
    --mode sensitivity \
    --param strength.energy_threshold_center \
    --min 2.0 \
    --max 4.0 \
    --steps 20
```

**预期发现**:
- 找出匹配率最高的阈值点
- 观察阈值变化对整体匹配率的影响
- 识别敏感区域

#### Step 2: phase_transition_width 敏感度分析

```bash
python3 scripts/strength_parameter_tuning.py \
    --mode sensitivity \
    --param strength.phase_transition_width \
    --min 5.0 \
    --max 20.0 \
    --steps 15
```

**预期发现**:
- 过渡带宽度对边界案例的影响
- 最优过渡带宽度

#### Step 3: GAT参数分析（如果启用）

```bash
python3 scripts/strength_parameter_tuning.py \
    --mode sensitivity \
    --param gat.attention_dropout \
    --min 0.1 \
    --max 0.5 \
    --steps 10
```

---

### 方案2: 网格搜索优化（在敏感度分析后）

**目的**: 找到最优参数组合

```bash
python3 scripts/strength_parameter_tuning.py --mode optimize
```

**默认搜索范围**:
- `energy_threshold_center`: 2.0 ~ 4.0 (10步)
- `phase_transition_width`: 5.0 ~ 20.0 (8步)

**总组合数**: 80个

---

### 方案3: 针对性优化（处理特定案例）

基于问题案例分析，可能需要：

1. **调整极弱/从格判定阈值**
   - 案例: 迈克尔·乔丹 (Weak → Follower)
   - 需要: 更低的Follower判定阈值

2. **调整临界案例的阈值**
   - 案例: 比尔·盖茨 (Weak → Strong, 42.7分)
   - 需要: 降低threshold_center，或调整判定逻辑

3. **处理"Moderate"标签**
   - 案例: 埃隆·马斯克、希特勒 (Moderate → Weak)
   - 需要: 确保Weak和Strong之间的过渡区合理

---

## 📋 调优步骤（推荐执行顺序）

### 第一步: 基线测试 ✅ 已完成

```bash
python3 scripts/strength_parameter_tuning.py --mode test
```

**结果**: 62.5% 匹配率

---

### 第二步: energy_threshold_center 敏感度分析

```bash
python3 scripts/strength_parameter_tuning.py \
    --mode sensitivity \
    --param strength.energy_threshold_center \
    --min 2.5 \
    --max 3.5 \
    --steps 20
```

**分析重点**:
- 观察匹配率曲线
- 找出峰值点
- 记录最优值

---

### 第三步: phase_transition_width 敏感度分析

```bash
python3 scripts/strength_parameter_tuning.py \
    --mode sensitivity \
    --param strength.phase_transition_width \
    --min 8.0 \
    --max 15.0 \
    --steps 14
```

---

### 第四步: 联合优化（网格搜索）

```bash
python3 scripts/strength_parameter_tuning.py --mode optimize
```

**注意**: 这可能需要较长时间，建议先用小范围测试

---

### 第五步: 验证和应用

1. 验证最优参数在所有案例上的表现
2. 检查是否有回归
3. 如果满意，更新配置文件

---

## 📈 预期改进

### 基于当前问题的预期改进

| 问题类型 | 当前数量 | 预期改进后 | 改进方法 |
|---------|---------|-----------|---------|
| **明显误判** (Strong↔Weak) | ~5个 | 0-1个 | 调整threshold_center |
| **边界案例** (Moderate相关) | ~5个 | 2-3个 | 调整transition_width |
| **从格识别** | 1个 | 0个 | 优化从格判定逻辑 |
| **其他误判** | ~4个 | 1-2个 | 综合优化 |

**预期总匹配率**: 75-85%

---

## ⚠️ 注意事项

### 1. 避免过度拟合

- ✅ 关注全局匹配率，而非单个案例
- ✅ 如果某个参数值对单个案例很好但全局下降，放弃该值
- ✅ 保持参数在合理物理范围内

### 2. 回归检查

每次参数调整后：
- ✅ 确保没有案例从匹配变为不匹配
- ✅ 如果出现回归，考虑回滚

### 3. 参数约束

**energy_threshold_center**:
- 理论范围: 1.0 ~ 5.0
- 推荐范围: 2.5 ~ 3.5（基于当前基线）
- 物理意义: 身强身弱临界点

**phase_transition_width**:
- 理论范围: 1.0 ~ 50.0
- 推荐范围: 8.0 ~ 15.0
- 物理意义: 过渡带宽度

---

## 📝 调优记录模板

```markdown
### 调优记录 - [日期]

**基线性能**: 62.5% (25/40)

**调优步骤**:
1. [参数名称] 敏感度分析
   - 范围: [min] ~ [max]
   - 最优值: [value]
   - 匹配率提升: [delta]%

2. [参数名称] 敏感度分析
   - ...

**最终结果**:
- 匹配率: [final_rate]%
- 最优参数:
  - energy_threshold_center: [value]
  - phase_transition_width: [value]
  - ...

**问题案例改善**:
- [案例名称]: [之前] → [之后] ✅/❌

**回归检查**: ✅ 通过/❌ 有回归
```

---

## 🚀 快速开始命令

```bash
# 1. 基线测试（已完成）
python3 scripts/strength_parameter_tuning.py --mode test

# 2. energy_threshold_center 敏感度分析（推荐先执行）
python3 scripts/strength_parameter_tuning.py \
    --mode sensitivity \
    --param strength.energy_threshold_center \
    --min 2.5 \
    --max 3.5 \
    --steps 20

# 3. 查看结果，找出最优值
# 4. 如果还有时间，进行 phase_transition_width 分析
# 5. 最后进行联合优化
```

---

## 📚 相关文档

- [V10.0 量子验证页面旺衰判定基础参数调优指南](./V10_QUANTUM_LAB_STRENGTH_TUNING_GUIDE.md)
- [V10.0 旺衰判定参数调优工作流](./V10_STRENGTH_TUNING_WORKFLOW.md)
- [V10.0 量子验证页面 MVC 架构文档](./V10_QUANTUM_LAB_MVC_ARCHITECTURE.md)

---

**维护者**: Bazi Predict Team  
**最后更新**: 2025-01-17

