# V60 Change Log

## 2026-07-30

### Verifiable Mingli attention Decision trace

- Added `v60.mingli-decision-trace.001`, a server-owned read projection over
  the existing immutable Cognition DecisionRecord. It verifies the stored
  record Hash, rebuilt request identity, route authority/status, complete
  attention-candidate coverage, selected-candidate evidence coverage and
  Proposal/Gate identity; any mismatch fails closed.
- Made the Decision scope explicit as
  `STATIC_NATAL_MECHANISM_CANDIDATE_PRIORITY_ONLY`. Attention-comparison
  eligibility is not professional admission and does not establish relation
  effect, source usability, timing activation, effective work, probability,
  auspiciousness or life outcome.
- Disclosed that the current Decision admits only mechanism-candidate evidence.
  Source usability, timing activation, the mechanism qualification matrix,
  professional admission and calibration are visible product context but are
  not bound Decision inputs.
- Clarified that the trace verifier rebuilds the canonical Kernel route while
  `EpistemicGate` checks bounded LLM route authorization, request association,
  candidate review and evidence-reference coverage. Provider/model/prompt/
  context identity belongs to the Hash-bound Reasoner Proposal, and none of
  these layers certifies professional correctness. Provider
  `counter_evidence_refs` remain request-bound references rather than
  professionally admitted counter-evidence. LLM confidence remains
  uncalibrated and hidden; a Rule Engine route records no Provider confidence
  or evidence citation and requires no Gate.
- Mingli Calculation, Abu Says and Lab now hand off the same Decision Ref/Hash,
  Reading binding, evidence coverage and unresolved qualification conditions.
  Lab expands the missing conditions while all three surfaces preserve the
  same non-verdict boundary.
- Added no canonical Mingli write and no database migration. Advanced Mingli to
  `v60.mingli-cognitive-engine.019`, the product units to
  `v60.unit-mingli.013`, `v60.unit-abu-says.006` and
  `v60.unit-lab.010`, and Runtime Architecture to
  `v60.runtime-architecture.040`.
- Passed the full 227-test backend suite, Ruff, TypeScript/Vite build, Runtime
  Architecture, source maintainability and real Chrome audits. The persisted
  QA Decision proves 4/4 candidate and evidence coverage, stable refresh,
  identical three-surface identity and no read-time comparison write. A second
  four-candidate Reading proves the visible `NOT_RUN` state without triggering
  comparison. A server-rendered contract also locks Rule Engine output and
  zero-, one- and multi-candidate pending presentation. Visual evidence is in
  `.artifacts/mechanism-decision-trace/`.

### Source-usability discussion prerequisites

- Added one immutable, read-only prerequisite projection over the persisted
  source-coordinate review vector.
- Grouped source candidates by visible carrier and exposed two unselected
  research scopes: exact stem identity only, and exact identity plus
  same-element/different-stem affinity.
- Made six verification dimensions explicit before relation effect or source
  usability may be discussed: match-scope, configuration-specific effect,
  seasonal capacity, multi-source aggregation, usability and professional
  admission. A dimension can remain `NOT_TRIGGERED` instead of being
  mislabelled as a current evidence gap.
- The current review Case now shows four visible carriers. The exact-identity
  scope contains six sources with one relation review; the inclusive scope
  contains ten sources with three relation reviews. Three carriers retain
  competing scopes and zero carriers receive a usability verdict.
- Mingli Calculation gives the compact comparison and Lab expands the same
  server-owned carrier scopes and verification states. No browser-side grouping,
  score, probability, effective-work claim or canonical write was added.
- Reused Reading v6 and the existing append-only source-review Ref/Hash; no
  database migration or V50 Runtime change was required.
- Verified 224 backend tests, Ruff, the production web build, Runtime
  Architecture, source maintainability and a real Chrome audit of the current
  `6/5/1` versus `10/7/3` sample. The audit found no console/network failure,
  document overflow or refresh identity drift.

### Source-coordinate relation review

- Added one hash-locked, Owner-bounded Profile for reviewing whether a
  deterministic source/manifestation coordinate intersects an admitted natal
  six-clash or six-harmony membership fact.
- Persisted the result as an append-only vector and bound new Mingli Readings,
  Mingli Calculation and Lab to the same Ref and Hash.
- Kept root usability, strength, relation effect, capacity, effective work,
  probability and canonical writes explicitly unresolved.
- The owner-facing Mingli surface now gives a compact clear/review split; Lab
  expands the exact source and peer coordinates without recomputing them in
  the browser.
- Qualified 23 authorized owner/reference Cases: all contain source evidence,
  10 have no admitted relation intersection and 13 require review. Across 188
  source candidates, 59 intersect an admitted six-clash or six-harmony fact.
  No professional verdict or probability was emitted.
- Applied migration `0021_source_coordinate_review`, added a repeatable
  desktop Chrome identity/authority audit, and preserved exact Reading v1-v5
  replay while issuing current Readings as v6.
- Completed 220 backend tests, Ruff, TypeScript/build, Runtime Architecture,
  source maintainability and browser console/network verification.
- Advanced Foundation to `v60.foundation.013`, Mingli to
  `v60.mingli-cognitive-engine.017`, Knowledge to
  `v60.knowledge-authority.007`, the product units to
  `v60.unit-mingli.011` / `v60.unit-lab.008`, and Runtime Architecture to
  `v60.runtime-architecture.038`.

### Candidate evidence-depth contrast

- Added a versioned, read-only Candidate Evidence Depth projection over the
  current Reading, Quant, Mechanism and Timing identities.
- Bound visible/hidden role carriers, month-branch coordinates, exact and
  same-element source evidence, timing overlaps, timing relations and shared
  competing participants without deriving a support score.
- Mingli now offers a compact explanation of why candidates remain in
  competition; Lab expands the same server-owned projection.
- An existing Reasoner Decision controls only attention labelling. It cannot
  become strength, effective work, probability or professional admission.
- Verified stable projection and lineage isolation across 23 active private
  owner/reference Cases. No database migration or V50 Runtime change occurred.
- Added a repeatable desktop Chrome audit for shared projection identity,
  visible candidate contrast, no authority overreach, document dimensions and
  console/network failures.
- Split the frontend mechanism contracts out of `homeApi.ts`; all Runtime
  source files remain inside the enforced maintainability budget.
- Completed 214 backend tests, TypeScript/build, Ruff, Runtime Architecture
  and source-maintainability verification.
- Advanced Mingli to `v60.mingli-cognitive-engine.016`, the product units to
  `v60.unit-mingli.010` and `v60.unit-lab.007`, and Runtime Architecture to
  `v60.runtime-architecture.037`.

## 2026-07-29

### Mechanism evidence-completeness projection

- Added one server-owned, versioned eight-part qualification matrix for every
  visible mechanism candidate.
- Each dimension now exposes present evidence, missing evidence and a concrete
  falsifier without calculating a score.
- Kept effect, capacity, usability and professional admission explicitly
  unadmitted; candidates cannot become effective work or probabilities.
- Bound Mingli Calculation, Mingli Lab and Abu to the same qualification Ref
  and Hash.
- Added deterministic cross-case and lineage-isolation tests over four
  persisted real owner Cases.
- Added a repeatable desktop audit for shared identity, evidence gaps,
  falsifiers, no professional overreach, document dimensions and runtime
  errors.
- Advanced Mingli to `v60.mingli-cognitive-engine.015`, the product units to
  `v60.unit-mingli.009`, `v60.unit-abu-says.005` and
  `v60.unit-lab.006`, and Runtime Architecture to
  `v60.runtime-architecture.036`. No database migration was required.

### Inspectable Mingli evidence explanations

- Added one server-owned, versioned explanation for the current Reading.
- Separated confirmed facts, candidate interpretations and real-world
  observation windows instead of blending them into a single answer.
- Bound every claim to support citations, counter-evidence status, unresolved
  questions, active Profile references and a plain-language boundary.
- Explicitly treats missing counter-evidence admission as unknown rather than
  positive support.
- Mingli Calculation, Mingli Lab and Abu now consume the same explanation
  identity; no product surface performs a second reading.
- Dream questions now disclose their already-public baseline rationale before
  AnswerSeal while keeping future evidence and outcome labels absent.
- Added a repeatable desktop browser audit covering explanation identity,
  candidate boundaries, pre-outcome Dream isolation, document dimensions and
  console/network failures.
- Advanced Mingli to `v60.mingli-cognitive-engine.014`, the product units to
  `v60.unit-mingli.008`, `v60.unit-dream.012`,
  `v60.unit-abu-says.004` and `v60.unit-lab.005`, and Runtime Architecture to
  `v60.runtime-architecture.035`. No database migration was required.

### Real owner Mingli control and repeatable Dream visits

- Added a real owner-facing Case intake path for solar/lunar birth input,
  including lunar leap month, birth time, place and timezone.
- New Cases compile through the existing deterministic calendar, Case,
  LifeCase, Fact and Canonical Scene owners. The browser never constructs a
  chart, Fact or Case identity.
- Added server-authoritative switching among a user's persisted owner Cases.
  Exactly one owner Case is active at a time, with account-scoped transaction
  fencing and no deletion or rewriting of prior Cases.
- The Home reading now states its formal Fact, candidate and timing coverage
  and calls itself a bounded evidence reading rather than a calibrated
  probability or professional verdict.
- Added `RETURN_TO_GROVE` after a completed Dream Encounter. It preserves the
  completed Encounter and all Seals/Reveal history, marks only the temporary
  navigation state as departed, and allows selection of another tree.
- Verified in desktop Chrome that a completed QA Encounter returns to an
  exactly-three-tree Grove, starts a new Encounter and restores that same
  Encounter after refresh with no console or network failures.
- Split Case UI styles, Runtime boundary presentation and Dream persistence
  responsibilities so every Runtime source file remains within its enforced
  maintainability budget.
- Advanced Mingli to `v60.mingli-cognitive-engine.013`, Dream to
  `v60.dream-game-engine.013`, the product units to
  `v60.unit-mingli.007` / `v60.unit-dream.011`, and Runtime Architecture to
  `v60.runtime-architecture.034`. Foundation remains
  `v60.foundation.012`; no database migration was needed.

### Readable Mingli brief and current-world Dream opportunities

- Added a deterministic, human-readable Mingli brief over the existing
  Reading, Fact, quantitative, timing and mechanism lineage.
- Added an explicit `核查结构主线` action to Mingli Calculation. It uses the
  configured `gemma4:latest` product Reasoner only when deterministic rules
  cannot rank the admitted candidates, then persists the gated comparison in
  the immutable Decision ledger.
- The selected attention path now shows its direct-fact, relation-context,
  visible-participant and hidden-participant support plus every unresolved
  professional condition. It remains clearly labelled as an investigation
  priority rather than effective work or a life conclusion.
- Corrected local startup to use the managed Runtime host, which restores the
  hash-locked Gemma4 serving profile instead of silently running the web
  product with the Reasoner unconfigured.
- The product now shows confirmed pillars, unweighted element membership,
  current Dayun/year/month coordinates, bounded mechanism attention and three
  life-domain observation windows without claiming strength, capacity,
  effective work, probability or auspiciousness.
- Made a fresh Dream tree selection materialize a new immutable question at
  the current authoritative World Tick instead of replaying an already-mature
  authored outcome.
- The selected opportunity commits its historical baseline through the
  existing World owner, schedules its unknown outcome five World Ticks later
  and is admitted through the existing Story owner.
- Kept baseline source-ref lineage in the server-only WorldEvent payload. The
  public evidence channel validates against the strict no-credit evidence
  contract and cannot carry provenance extensions or future outcome evidence.
- Verified the complete desktop path in the browser: three-tree selection,
  two leaves and one branch, one AnswerSeal, waiting across refresh and a full
  process restart, independent World settlement, Reveal, reconciliation and
  completed-state recovery with a clean console.
- Verified that all three Grove source lives independently materialize a
  distinct current-World question, preserve their source-question identity
  and receive a later due Tick; none reuses an already-mature outcome.
- Split Grove selection and Home Reading types into bounded owner files so the
  Dream service and web API types remain within source-size limits.

### Real Mingli corpus qualification and three-life Dream Grove

- Imported and independently compiled 20 authorized V50 owner/reference
  profiles into V60 without introducing a V50 Runtime dependency or rewriting
  an existing V60 LifeCase.
- Persisted one append-only qualification run: all 20 cases have timing
  coordinates and mechanism candidates, 10 have admitted structural
  candidates, and none emits a probability or professional verdict.
- Added three independent canonical synthetic lives for career, wealth and
  relationship: 闻溪、禾央 and 照宁. Each owns a distinct Case, committed
  timeline, persistent LifeTree, phenotype projection and life question.
- Replaced the single default Dream entry for fresh visitors with one
  server-authoritative Grove containing exactly three anonymous selectable
  trees. Existing encounters, including the completed 砚舟 lineage, remain
  unchanged and recoverable.
- Completed the 禾央 path in the desktop product through evidence, branch,
  question, AnswerSeal, world evidence, Reveal, reconciliation, browser
  refresh and a full Runtime stop/restart.
- Split Dream command validation, the story canvas and HTTP endpoint clients
  into bounded owner files; all Runtime source files remain within the
  maintainability budget.
- Applied migration `0020_three_life_qualification` and advanced Foundation to
  `v60.foundation.012`, Mingli to `v60.mingli-cognitive-engine.012`, Dream to
  `v60.dream-game-engine.012` and Runtime Architecture to
  `v60.runtime-architecture.033`.

### Life-domain evidence and fifth Dream episode

- Added append-only career, wealth and relationship evidence vectors over the
  same real natal, mechanism and timing sources.
- Kept the outputs as bounded attention windows rather than probabilities,
  events, auspiciousness, capacity or professional conclusions.
- Advanced new Mingli Readings to v5 while preserving exact v1-v4 replay.
- Added the fifth Yanzhou Episode, where the committed public water record
  becomes historical baseline and a later roster-duty consequence remains
  independently unknown until World settlement.
- Preserved the fourth Episode package identity and linked the fifth chapter
  through the append-only external transition registry.
- Updated Home, Mingli and Lab to project the same domain vector and honest
  boundary; split the new Reading styles out of the existing scene stylesheet.
- Applied migration `0019_mingli_life_domains`, admitted the fifth source
  package and verified five Episodes/four transitions with zero source,
  authority or architecture drift.
- Verified the desktop Mingli domain windows and confirmed the current fourth
  Dream question remains unsealed through browser refresh and a full Runtime
  stop/restart.
- Advanced Foundation to `v60.foundation.011`, Mingli Reading to
  `v60.mingli-reading.005`, Mingli Engine to
  `v60.mingli-cognitive-engine.011`, Story to `v60.story-engine.008`, Dream to
  `v60.unit-dream.009` and Runtime Architecture to
  `v60.runtime-architecture.032`.

### Real Mingli timing evidence and fourth Dream episode

- Added a hash-locked Timing Evidence Profile and deterministic Dayun, annual
  and monthly compilation for real V60 LifeCases.
- Persisted append-only Timing Evidence Vectors and bound new Mingli Readings
  to the exact Profile/vector identity while preserving v1/v2/v3 replay.
- Added natal same-branch, six-clash and six-harmony membership evidence plus
  label-only overlap with existing mechanism candidates.
- Kept activation, effect, capacity, usable work, auspiciousness, reality
  events and probability explicitly unresolved.
- Verified Liu Jin's live coordinates as
  `庚子大运 / 丙午流年 / 乙未流月`.
- Added the fourth Yanzhou source package and transition. Its second evidence
  leaf is the frozen `壬辰 / 丙午 / 乙未` vector; the story outcome remains
  independent of that coordinate evidence and of the player's answer.
- Verified the fourth question in the desktop product through page refresh and
  full Runtime restart with clean browser and server logs.
- Advanced Foundation to `v60.foundation.010`, Mingli Reading to
  `v60.mingli-reading.004`, Mingli Engine to
  `v60.mingli-cognitive-engine.010` and Runtime Architecture to
  `v60.runtime-architecture.031`.

### Gemma4 product Reasoner correction

- Restored the previously frozen model-role contract: `gemma4:latest` is the
  V60 product expression/structured-decision model; Qwen remains a
  Research/Teacher model.
- Reused the V50 Ollama connection protocol through a native V60 provider:
  `http://dblife.com:11888/api/generate`, `gemma4:latest`, strict JSON Schema.
- Added hash-locked serving profile
  `v60.model-serving.gemma4-structured-decision.001` with `think=false`,
  `temperature=0`, `top_p=0.95`, `top_k=64`, `num_ctx=32768`,
  `num_predict=1200`, `keep_alive=30m` and managed timeout `180s`.
- Included the exact model profile identity and SHA-256 in proposals,
  DecisionRecords and the public Runtime manifest.
- Kept V50 as a read-only reference; V60 imports no V50 Runtime module.
- Made the managed local V60 service start with the dblife Reasoner enabled
  while preserving explicit environment overrides and fail-closed status.
- Ran the live Liu Jin mechanism comparison through the real model, existing
  Epistemic Gate and immutable Cognition ledger.
- Persisted Decision
  `v60-decision-c2d42a716d937334c728` with Hash
  `02e8068c1c75832758b5453d30025aa9ff92e8c66fa1d141d6c4c41431b7307f`.
- The selected result remains `ATTENTION_PRIORITY_ONLY`; no LLM output may
  write canonical Mingli facts or claim effective work.
- Preserved the earlier `qwen3.5:35b` Decision
  `v60-decision-ce903af868dd4cf5dd52` as append-only historical connection
  evidence rather than rewriting or presenting it as the current result.

### Mingli mechanism evidence v1 and real-data coverage

- Added a hash-locked Mechanism Evidence Profile with five bounded candidate
  patterns and explicit forbidden conclusions.
- Compiled real chart Facts and quantitative Ten-God occurrences into
  immutable mechanism vectors with source roles, relation context,
  competition and unresolved blockers.
- Persisted vectors in PostgreSQL, bound them into Mingli Reading v3 and
  retained Reading v1/v2 replay.
- Audited all 26 active V50 profiles read-only: 26 distinct charts, one to four
  candidates per profile, all five patterns represented and stable replay
  Hashes throughout.
- Materialized mechanism vectors for every active V60 Case.
- Connected candidate comparison to the bounded Reasoner, Epistemic Gate and
  Cognition ledger. The later dblife activation above completes the live model
  path without changing its authority boundary.
- Added the minimal Lab display and split its command/style code to preserve
  source-size budgets.
- Advanced Foundation to `v60.foundation.009`, Mingli to
  `v60.mingli-cognitive-engine.009` and Runtime Architecture to
  `v60.runtime-architecture.030`.

### Quantitative Mingli foundation v1

- Added the versioned Bazi Quant Foundation Profile with complete
  five-element cycle and 100-pair Ten-God stem mappings.
- Compiled canonical chart Facts into an immutable, append-only quantitative
  vector covering element and polarity membership, all Ten-God occurrences
  and bounded source/manifestation evidence.
- Bound the exact Quant Profile and vector identities into Mingli Reading v2
  while preserving byte-stable Reading v1 replay.
- Added append-only PostgreSQL persistence and Profile/vector Hash validation;
  changed Profiles create new vector identities rather than rewriting history.
- Projected the same vector into Mingli Calculation and Lab. The desktop
  product explicitly labels it `非旺衰 · 非概率` and keeps root usability,
  seasonal authority, capacity, effective work and probability unresolved.
- Verified the live Liu Jin Reading against PostgreSQL in the browser with a
  clean console, then advanced Foundation to `v60.foundation.008`, Mingli to
  `v60.mingli-cognitive-engine.008` and Runtime Architecture to
  `v60.runtime-architecture.029`.

### Mingli core and quantitative direction

- Audited the admitted V60 Mingli surface against the reusable V50 knowledge,
  graph, mechanism, timing, portrait and Agent assets.
- Confirmed that the current V60 Runtime is reproducible but intentionally
  narrow: advanced Ten-God, source/root, effect, capacity, timing and
  life-domain conclusions remain unadmitted.
- Preserved `CognitiveDecisionKernel` as the single decision router: system and
  rules resolve what they can prove, a bounded LLM may compare only qualified
  candidates, human authority owns consent, and insufficient evidence remains
  unresolved.
- Defined separate contracts for deterministic measurements, uncalibrated
  support scores, calibrated probabilities and decision-quality evaluation.
- Assigned experimentation, backtesting and calibration to Lab, with admitted
  immutable Profile versions affecting only future Readings.

### Mingli-centered five-unit model and shared Reading

- Made Mingli Calculation the machine-readable product core and kept Abu
  Dream as the priority game breakthrough.
- Defined Lab as the versioned research-improvement loop, Abu Says as the
  native expression of one Reading and Theater as the relatively independent
  directing and media Studio.
- Added `MingliReadingEnvelope`, binding the Home Case to exact chart,
  LifeCase, Knowledge Profile, Fact, candidate, Decision and unresolved-state
  identities.
- Made Mingli, Lab and Abu share one `reading_ref`; Abu can no longer present
  an unbound parallel reading on the Home surface.
- Kept candidate effect, capacity, usability, timing and professional
  admission explicitly unresolved.
- Advanced Mingli to `v60.mingli-cognitive-engine.005` and Runtime
  Architecture to `v60.runtime-architecture.026`.

### Append-only Mingli Reading history

- Persisted each shared Mingli Reading as an immutable, profile-pinned record.
- Added database enforcement preventing Reading update or deletion.
- Made repeated Home projection idempotent and exact historical Reading
  replay verifiable.
- Advanced Foundation to `v60.foundation.007`, Mingli to
  `v60.mingli-cognitive-engine.007` and Runtime Architecture to
  `v60.runtime-architecture.028`.
- Centralized the exact active Knowledge and candidate-rule Profile selection;
  Lab research cannot affect a new Reading until that hash-locked selection
  explicitly changes.
- Made deterministic Fact compilation derive its executable maps from that
  same selected Foundation Profile instead of unrelated module globals.

### Private Home LifeTree and explicit Dream threshold

- Changed the default signed-in experience from a synthetic Dream subject to
  Liu Jin's private `HUMAN_OWNER` LifeTree.
- Added a fail-closed, read-only Home projection built from the formal chart,
  LifeCase, Canonical Scene and versioned tree phenotype.
- Made entering Dream explicit and kept Yanzhou's continuing canonical
  synthetic encounter separate from the viewer's own life.
- Made all five product units follow the active Home or Dream life line
  without sharing Case references across scopes.
- Added return-to-Home, browser-back, refresh, direct-Dream restoration and
  process-restart recovery without resetting the Dream world.
- Verified the desktop journey in the browser with no document scroll,
  console warning, failed request or canonical Home write.
- Added the product and authority contract in
  `docs/14_V60_PRIVATE_HOME_TREE_AND_DREAM_THRESHOLD.md`.

### One Life Event across five product units

- Connected the revealed StoryFruit to its committed WorldEvent evidence, so
  Theater now replays the two facts that actually matured that fruit.
- Added bounded, focus-aware Abu explanations for meaning, uncertainty and
  next attention without introducing an LLM or a new fact source.
- Kept world-event focus out of Mingli truth and Lab candidates while
  retaining the shared Life-Line context.
- Replaced raw default provenance and unresolved status strings with readable
  product language; exact source receipts remain available on demand.
- Made the shared focus bidirectional: Mingli facts return to the structure
  leaf, Lab candidates return to the structure branch and committed Theater
  evidence returns to its evidence leaf or revealed fruit.
- Preserved bidirectional focus through URL refresh and browser history without
  creating a canonical write or inferring an unreferenced tree location.
- Verified the completed third Episode across all five units at 1280x720,
  including refresh recovery, no document scroll and zero console warnings or
  errors.

### Enforced source modularity

- Split the 1610-line Dream service into command orchestration,
  `DreamRepository`, `DreamSnapshotProjector` and
  `DreamOutcomeCoordinator` without changing its public API or state machine.
- Split login, brand and companion presentation out of `App.tsx`.
- Split the 1629-line stylesheet into five ordered, responsibility-based CSS
  modules with no selector or value changes.
- Added an executable Runtime source-size audit: Python 850 lines, TS/TSX 500
  lines and CSS 600 lines are hard ceilings.
- Added architecture tests proving Dream read, write and outcome owners do not
  collapse back into the command service.

## 2026-07-28

### Reveal-safe Theater evidence projection

- Split Experience Context evidence into cutoff-bounded `baseline_evidence`
  and post-cutoff committed `revealed_evidence`.
- Kept `revealed_evidence` absent until a Reveal exists; sealed outcome atoms
  and hidden NPC choices remain excluded.
- Changed Theater to stage actual committed outcome evidence after Reveal
  instead of repeating the no-credit baseline clues.
- Added cutoff, current-world-horizon, overlap and phase-gate validation plus
  end-to-end Dream projection coverage.
- Verified the completed third Episode after a real process restart: Theater
  showed the two night-water outcome facts, the page remained 1280x720
  without document scroll and the browser console stayed clean.
- Advanced Experience Context to `v60.experience-context.003`, Theater to
  `v60.unit-theater.003` and Runtime Architecture to
  `v60.runtime-architecture.025`.

### Third Episode and Registry-only Story authority

- Added the third canonical Episode,
  `v60-dream-episode-yanzhou-shared-night-water-v1`, as a hash-locked source
  package with its own admitted WorldEvent and outcome contract.
- Added the second append-only transition without rewriting either earlier
  Episode package or persisted contract.
- Shifted the question from tree mechanics to an Actor promise: whether
  砚舟 will share the next night water before reinforcing his own bank.
- Verified a deliberately incorrect player judgment reaches
  `NOT_SUPPORTED` only after the independently settled public action.
- Made Registry transitions authoritative when an immutable source package has
  no embedded continuation pointer.
- Converted `first_slice.py` and `return_slice.py` into Registry-backed
  compatibility views with no authored Chinese content.
- Removed the source exporter that could overwrite the Registry and replaced
  it with a read-only audit tool.
- Seed now resolves its successor through Registry edges and reports all
  admitted Question Refs plus Registry Hash
  `10a8bf8661bba6744731839e6ed798477aee84b6f0362d410597dc21231420c9`.
- Advanced Story Engine to `v60.life-story-engine.007`, Dream Product Unit to
  `v60.unit-dream.007` and Runtime Architecture to
  `v60.runtime-architecture.024`.

### Versioned Episode source packages

- Exported the two existing encounters into a hash-locked Story source
  registry under `content/dream/episodes/`.
- Bound each Episode package to its WorldEvent definitions and moved the
  explicit first-to-return edge into the Registry.
- Added strict package compilation with explicit canonical bindings and
  fail-closed checks for registry drift, package drift, path traversal,
  malformed markers, inexact binding sets, event drift and transition drift.
- Proved both packages compile to the existing Episode definitions without
  changing persisted Question, organ, outcome or graph identities.
- Replaced Seed's authored event and transition branches with Registry-driven
  loops that still call the existing World and Story write owners.
- Kept raw packages outside the live Runtime path; Dream still reads only
  admitted PostgreSQL Episode contracts.
- Advanced Story Engine to `v60.life-story-engine.006` and Runtime
  Architecture to `v60.runtime-architecture.023`.

### Candidate qualification boundary

- Added a hash-locked candidate qualification Profile owned by Knowledge.
- Added deterministic Mingli qualification receipts binding candidate, rule,
  evidence, evaluated claims, forbidden conclusions and receipt Hash.
- Limited the first executable rule to relation-membership structure
  visibility with `selection_authority=false`.
- Source drift, missing claims and unadmitted fact types now fail closed.
- Lab now distinguishes verified structure evidence from unresolved effect,
  capacity, timing and professional admission.
- Advanced Mingli to `v60.mingli-cognitive-engine.004`, Knowledge to
  `v60.knowledge-authority.003`, Lab to `v60.unit-lab.003` and Runtime
  Architecture to `v60.runtime-architecture.021`.

### Bounded Reasoner Runtime Host

- Added exact Candidate/Evidence context contracts for model-visible input.
- Added `CognitiveDecisionCoordinator` as the shared execution path for
  deterministic decisions and authorized bounded comparisons.
- Added an optional OpenAI Responses adapter using strict structured output,
  no tools, `store=false`, explicit timeout and fail-closed configuration.
- Runtime now stamps provider, model, prompt, response and context identities
  before `EpistemicGate` admission.
- Existing DecisionRecords are recovered before provider invocation, avoiding
  a second model call on refresh or retry.
- Kept the local provider honestly `NOT_CONFIGURED`; no network request can
  occur without provider, model, API key and explicit enablement.
- Advanced Decision Kernel to `v60.cognitive-decision-kernel.004` and Runtime
  Architecture to `v60.runtime-architecture.020`.

### Append-only Episode transitions

- Added `EpisodeTransitionContract` and
  `StoryEpisodeTransitionAdmissionService` as the sole append-only owner for
  Story graph edges.
- Added migration `0014_episode_transitions`, backfilling the existing
  first-to-return edge while preserving all Episode and gameplay record
  Hashes.
- Removed Runtime and test fallback to legacy Episode continuation fields;
  the active catalog now requires explicit transition rows.
- Added regression coverage proving a third Episode can be appended without
  mutating the preceding immutable Episode contract.
- Unified emitted ScenePlan Story version with the system Manifest.
- Advanced Foundation to `v60.foundation.006`, Story Engine to
  `v60.life-story-engine.004`, Dream Game Engine to
  `v60.dream-game-engine.011`, Runtime Architecture to
  `v60.runtime-architecture.019` and Dream Product Unit to
  `v60.unit-dream.006`.

### Episode-driven tree progression

- Removed authored tree-state names and version numbers from Dream Service.
- Derived entry and settlement tree versions from the validated active
  Episode graph, preserving idempotency and rejecting version gaps.
- New Episodes can now extend tree progression through admitted content
  without modifying the Dream command engine.
- Made the browser first-slice audit complete Reconciliation before asserting
  that a later Episode can be entered.
- Removed first/return authored copy branches from the LifeTree scene; visible
  phase copy now comes only from the validated Episode Narrative projection.
- Advanced Dream Game Engine to `v60.dream-game-engine.010`, Runtime
  Architecture to `v60.runtime-architecture.018` and Dream Product Unit to
  `v60.unit-dream.005`.

### Episode-scoped public disclosure

- Added `EpisodePublicProjection` as the Runtime boundary between mutable
  canonical Actor/LifeTree state and one viewer's current Episode.
- Filtered Actor timelines to the current baseline and authorized Reveal
  event; mutable Actor state now fails closed when later events exist.
- Replaced the exposed global LifeTree version/state with a deterministic
  Episode-local projection and Hash.
- Added regression coverage proving a fresh first visit cannot observe
  already-settled return-visit content.
- Updated browser slice audits to wait for World-owned settlement and inspect
  Episode-local tree projection identity instead of retired browser world
  controls and global tree versions.
- Advanced Dream Game Engine to `v60.dream-game-engine.009` and Runtime
  Architecture to `v60.runtime-architecture.016`.

### World-owned canonical waiting

- Removed `ADVANCE_WORLD` from the public Dream command contract and canonical
  scene controls.
- Made `WAITING_FOR_WORLD` read-only: the browser polls the committed
  Encounter while the process-owned World Runtime advances database time and
  settles due events.
- Added contract coverage rejecting the retired client command and changed
  full-loop tests to use World settlement plus idempotent Dream catch-up.
- Added database Foundation identity verification to migration `0013`, health
  checks and the managed local Runtime guard.
- Advanced Dream Game Engine to `.008` and Runtime Architecture to `.015`;
  Foundation remains `.005`.

### Immutable Dream command receipts

- Added `dream.command_receipts` through migration
  `0013_dream_command_receipts`.
- Bound every accepted Dream command to its exact envelope Hash and committed
  result Encounter/version/state Hash in the same transaction.
- Changed semantic replay from state inference to immutable receipt identity:
  exact replay succeeds, stale fresh commands fail closed and changed
  idempotency reuse conflicts.
- Added concurrent duplicate-command coverage and Runtime receipt-integrity
  auditing.
- Advanced Foundation to `.005`, Dream Game Engine to `.007` and Runtime
  Architecture to `.014`.

### Versioned Dream command boundary

- Replaced separate mutable Dream API routes with one
  `/api/v60/dream/command` endpoint.
- Added a strict `DreamCommandEnvelope` binding Encounter identity, expected
  version, idempotency identity and command-specific payload.
- Added server-side phase and organ-role validation so presentation cannot
  invoke a transition that the Gameplay Director did not expose.
- Added semantic replay for already-observed organs, settled events, Reveal,
  reconciliation and continuation while retaining persisted AnswerSeal
  idempotency.
- Added a disposable PostgreSQL full-loop test covering both chapters, stale
  version rejection and exact command replay.
- Advanced Foundation to `.004`, Dream Game Engine to `.006` and Runtime
  Architecture to `.013`.

### Executable Knowledge Authority

- Added `abu_v60.knowledge` as the read-only owner of admitted, versioned
  knowledge profiles.
- Moved the Bazi foundation tables behind an immutable Profile and exact
  ID/version/SHA-256 resolver while preserving the established source Ref and
  compiled chart output.
- Exposed only the admitted Profile manifest through Runtime diagnostics.
- Locked the current Profile Hash to
  `d02eb38e8a65882951dc2668b2d5aeb404902489d2d5441c915437b3a81452bb`.
- Preserved `professionally_reviewed=false` and all forbidden-inference
  boundaries; no database, question bank or LLM path was added.
- Advanced Foundation to `.003`, Knowledge Authority to `.002` and Runtime
  Architecture to `.012`.

### Bootstrap owner admission and replay boundary

- Replaced direct Seed and V50-import target writes with typed Platform,
  Identity, Mingli, World, Story and Dream admission ports.
- Added immutable Actor and LifeTree admission manifests through migration
  `0012_owner_admission`.
- Preserved mutable Actor timelines and evolved LifeTree state during exact
  Seed replay; existing trees are explicitly marked as migration backfills.
- Kept the original first-slice batch immutable and admitted return content
  through a separate extension batch receipt.
- Expanded Runtime integrity to reject unadmitted or invalid Actors and
  LifeTrees.
- Added negative tests for batch, Actor, Case, Tree and manifest drift plus
  static tests that forbid cross-schema bootstrap writes.
- Verified migration downgrade/upgrade and repeated Seed replay without
  changing the Event/Evidence/Seal/Fruit/Reveal/Actor business snapshot Hash.
- Advanced Foundation to `.002`, Identity to `.002`, Mingli to `.003`, World
  to `.004`, Dream Game to `.005`, Migration Boundary to `.002` and Runtime
  Architecture to `.011`.

### WorldEvent admission and idempotent historical continuity

- Added `WorldEventAdmissionCompiler` and `WorldEventAdmissionService` as the
  only admission path for authored events and initial evidence.
- Bound Actor, Case, branch, event payload, due Tick, sealed outcome and
  initial evidence to immutable definition and admission Hashes.
- Required scheduled events to retain system-owned outcomes and reject
  answer-dependent or incomplete future evidence.
- Revalidated the admission receipt under the World settlement lock.
- Removed direct WorldEvent/EventEvidence writes from Dream seeding.
- Made historical event replay idempotent at both event and Actor timeline
  boundaries.
- Added migration `0011_world_event_admission`; preserved all prior WorldEvent,
  AnswerSeal, Fruit and Reveal identities and Hashes.
- Advanced World Continuity Engine to `.003` and Runtime Architecture to
  `.010`.

### Deterministic Episode admission and resolution boundary

- Added `EpisodeAdmissionCompiler` and `StoryEpisodeAdmissionService` as the
  only playable Story-content write path.
- Bound Question, organ and Episode Hashes to the exact LifeCase revision,
  structure Fact, WorldEvent, cutoff, due Tick and sealed outcome Hash.
- Made exact admission replay idempotent and changed-content identity reuse a
  hard conflict instead of `ON CONFLICT DO NOTHING`.
- Removed direct Story SQL from Dream seeding and made Runtime Catalog
  revalidate every admission manifest and authority binding.
- Separated the formal Resolution Rule from organ, NPC, fruit and narrative
  metadata, eliminating historical mixed-payload drift.
- Added migration `0010_episode_admission_manifest` and preserved all existing
  Encounter, AnswerSeal, Fruit and Reveal identities and Hashes.
- Advanced Runtime Architecture to `.009` and Life Story Engine to `.003`.

### Versioned Episode narrative and phase disclosure

- Added `EpisodeNarrativeContract` with one authored moment for each of the
  six Dream phases.
- Centralized phase-specific title, status, Theater beat and Abu line in the
  persisted Episode contract; removed frontend and projector story branching.
- Added four explicit disclosure classes, including
  `WORLD_COMMITTED_HIDDEN` between settlement and Reveal.
- Advanced Experience Context to `.002` and made phase/disclosure mismatch a
  fail-closed validation error.
- Upgraded both active Episodes to contract v3 through migration `0009` while
  preserving existing Encounter, AnswerSeal, Fruit and Reveal identities and
  Hashes.
- Advanced the Runtime architecture to `.008`, Life Story Engine to `.002`
  and Dream Game Engine to `.004`.

### Shared experience context and disclosure boundary

- Added immutable `ExperienceContextEnvelope` as the only input to Dream,
  Mingli, Abu, Theater and Lab projectors.
- Bound Encounter, Actor, Tree, World, Case, LifeCaseRevision, ChartVersion,
  Scene, Question and WorldEvent identity into one stable context Hash.
- Added fail-closed validation for post-cutoff evidence, outcome atoms,
  pre-Reveal DecisionRefs, duplicate refs and non-monotonic progress.
- Added explicit per-unit disclosure manifests and shared `context_ref`
  verification across all five projections.
- Kept sealed outcomes and hidden NPC choices outside the shared context.
- Advanced the executable architecture to `v60.runtime-architecture.007`
  without adding a schema or canonical write owner.

### Executable Reasoner proposal and Gate boundary

- Added immutable `DecisionProposal` and `EpistemicGateReceipt` contracts.
- Added Gate validation for complete candidate comparison, qualified
  selection and request-bound evidence/counter-evidence.
- Added immutable Decision Ledger admission for validated proposals while
  preserving `canonical_domain_write_allowed=false`.
- Prevented pending or unresolved routes from being stored as final
  DecisionRecords.
- Exposed the honest local Reasoner state as `NOT_CONFIGURED` with network
  calls disabled and bumped the Decision Kernel to `.003`.

### Inspectable structural candidate projection

- Added a typed, deterministic `StructuralCandidateCompiler` for admitted
  六合/六冲 membership facts.
- Added stable candidate identity, participants, exact evidence/source refs
  and explicit missing professional requirements.
- Routed Lab candidates through the Cognitive Decision Kernel with no
  selection qualification and no LLM, preserving `UNRESOLVED / NONE`.
- Made the first real structural candidate visible in Mingli Lab without
  creating effective work, PathAssertion, DecisionRecord or LifeCase writes.
- Bumped the Mingli engine to `.002`, Lab unit to `.002` and executable
  architecture registry to `.005`.

### Continuous World Runtime

- Activated a rational PostgreSQL wall-clock epoch for the canonical Dream
  world without resetting its existing Tick or completed encounters.
- Added the process-owned Runtime worker and transaction-scoped advisory
  fencing around `WorldContinuityEngine` pulses.
- Added automatic due-event settlement plus Dream projection catch-up that
  recovers after a process crash without duplicating Fruit or Encounter
  versions.
- Exposed worker health and counters in Runtime status and advanced the World
  engine identity to `v60.world-continuity-engine.002`.
- Verified exact integer clock projection, no rewind, clean shutdown, local
  migration, transactional rollback audit and the complete regression suite.

### Executable Runtime media authority

- Added a fail-closed Media Runtime Resolver over the hash-verified Media
  Catalog and Asset Registry.
- Added admitted IDLE and GUIDE_LEFT Cue Bundles and exposed only named,
  versioned media bindings through `/api/v60/bootstrap`.
- Removed registered asset path literals from React and PixiJS; brand, scene,
  motion and reduced-motion poster deliveries now come from the Registry.
- Added Runtime media health, path-boundary and Cue-resolution tests.
- Advanced the executable Media module to `v60.media-library.002` and the
  Asset Registry manifest identity to `v60.asset-registry.002`.

### Executable central decision authority

- Advanced the central decision identity to
  `v60.cognitive-decision-kernel.002`.
- Added the immutable `CognitiveDecisionLedger` as the only Cognition-schema
  writer.
- Routed scheduled WorldEvent outcomes and Reveal atom reconciliation through
  the Kernel before their owning transactions commit.
- Propagated committed Reveal decision references into the shared lineage,
  Theater and Story projections.
- Added exact-replay idempotency, changed-payload conflict and unique-writer
  tests.
- Extended the managed local Runtime guard to reject a stale version of any
  core engine, not only the Dream Game engine.
- Verified the complete Seal-to-settlement-to-Reveal path in a disposable
  PostgreSQL database: two records were created, retry remained at two, and
  the QA database was removed afterward.

### Gameplay and story foundation

- Added the single `DreamEpisodeCatalog` Runtime reader with full active-graph
  validation, deterministic graph Hash, retired-Episode replay support and
  fail-closed handling for missing, cyclic or unreachable content.
- Added a versioned `DreamSceneRegistry` and stable Scene identity for all six
  legal gameplay phases.
- Removed the remaining direct Episode SQL and per-question contract
  validation from `DreamService`; entry, continuation, Seal, settlement,
  snapshot and NPC Seal now resolve through the same catalog.
- Extended Runtime status with Episode Catalog and Scene Registry health.
- Added a PID-, version- and integrity-aware local Runtime manager that refuses
  stale or foreign services on port `8060`.
- Advanced the game engine identity to `v60.dream-game-engine.003`.
- Added versioned `DreamEpisodeDefinition` and persisted
  `DreamEpisodeContract` models.
- Added structural validation for the two-leaf, one-branch, one-flower,
  one-fruit loop, comparable answer atoms, complete future evidence and
  pre-Seal outcome isolation.
- Added `DreamGameplayDirector` to verify Episode Hashes and produce phases,
  legal commands and public organ states.
- Bound complete flower/fruit/Theater Runtime metadata and the persisted
  Resolution Rule Hash into Episode v2, removing the final first-visit
  metadata fallback.
- Migrated both existing QuestionInstances to immutable Episode contracts
  without changing existing AnswerSeal, Fruit, Reveal, Question or organ
  Hashes.
- Removed authored first/return Question branching from `DreamService` and
  removed Dream-content imports from `LifeStoryEngine`.
- Refactored seed admission so both chapters pass through the same complete
  Episode Definition.
- Advanced the game engine identity to `v60.dream-game-engine.002`.

### Executable product architecture

- Added a machine-readable Runtime architecture registry with startup
  validation for dependencies, unique schema owners and read-only product
  units.
- Split encounter rules, world continuity and story projection into
  `DreamGameEngine`, `WorldContinuityEngine` and `LifeStoryEngine`.
- Renamed the constitutional decision owner to `CognitiveDecisionKernel` while
  retaining a compatibility alias for existing callers.
- Added atom-level evidence reconciliation with no baseline credit and
  fail-closed missing evidence.
- Added separate Mingli, Abu Says, Theater and Lab projectors and composed all
  five product units from one Case/world lineage.
- Added a formal Mingli Calculation view distinct from the research-only Lab.
- Added a read-only Runtime integrity endpoint and command-line architecture
  audit.
- Added owner-boundary, game-state, reconciliation and story-engine tests.
- Verified all five desktop units, direct URL recovery, zero document scroll
  and a clean browser console on the local Runtime.

### Shared life-line focus and Encounter isolation

- Connected Dream, Abu Says, Theater and Mingli Lab through one selected tree
  organ and its server-owned source refs.
- Added URL-restorable `view` and `focus` navigation without making navigation
  state a canonical fact.
- Kept world evidence out of Mingli facts and filtered structure focus to the
  matching bounded formal fact.
- Bound immutable organ projections and hashes to each QuestionInstance.
- Stopped viewer observation, Seal, Reveal and reconciliation progress from
  mutating the canonical LifeTree or replacing its organ set.
- Made canonical LifeTree growth monotonic across first outcome, return action
  and return outcome.
- Added a repeatable desktop semantic-focus audit covering Theater/Lab source
  alignment, refresh recovery, document scroll and failed requests.
- Reserved future localization through language-neutral identity and stable
  content keys while keeping the current Runtime Chinese-only.

### GUIDE_LEFT runtime audit

- Added a repeatable authenticated browser audit for the one-shot
  `GUIDE_LEFT -> IDLE` transition.
- The audit verifies stable actor bounds, a scroll-free desktop viewport and
  zero failed runtime requests while preserving the Encounter as fact owner.

### Foundation

- Created a Python 3.12 modular-monolith runtime with an independent
  PostgreSQL database.
- Established the Dream-first product charter and Cognitive Decision Kernel.
- Added one canonical integer world clock and a versioned media registry.
- Imported only approved grove and Abu actor assets into V60.
- Added desktop, 390 px and reduced-motion visual audits for the empty-world
  foundation.

### First real vertical slice - local implementation complete

- Froze the V50 boundary: account verifier and birth input may cross; blocked
  LifeCase conclusions may not.
- Selected the Owner profile by explicit source ID, not by name guessing.
- Chose recomputation with strict pillar parity as the admission test.
- Implemented identity, session, Case, ChartVersion, bounded fact, LifeCase and
  Canonical Scene persistence.
- Imported the existing credential verifier and selected birth input, then
  independently recomputed the Owner chart as `丁巳 乙巳 乙丑 乙酉`.
- Added one canonical synthetic actor, one persistent LifeTree, one immutable
  QuestionInstance and one independently resolved WorldEvent.
- Implemented two AnswerSeals, sealed and matured StoryFruit states, Reveal,
  reconciliation, idempotent retry, refresh recovery and process restart
  recovery.
- Connected Dream, Abu, Theater and Lab as projections of the same
  LifeCase/Question/WorldEvent lineage.
- Imported the approved semantic-tree asset bundle into the V60 registry and
  removed the flower once its single fruit is formed.
- Completed a disposable-database Chrome audit with no console errors, failed
  requests, document scroll or pre-reveal future-evidence leakage.

### Decisions retained

- Dream World remains the primary product surface.
- Mingli calculation, Abu Says, Theater and Lab share one Case and one evidence
  chain.
- The system decides deterministic facts and world outcomes.
- Bounded LLM reasoning is permitted only after deterministic candidates are
  exhausted and can never write canonical facts directly.
- Human users own consent and personal choices.

### Versioned media production library

- Added immutable Gemini source ingest with ffprobe receipts and revision
  overwrite protection.
- Added a hash-verified production catalogue for source, master, review and
  Runtime delivery lineage.
- Added audio/visual Cue Bundles so actor video and audio remain independently
  mixable and versioned.
- Registered approved ABU_01 idle, ABU_02 follow-walk and Morning Glints web
  deliveries; ABU_03 remains blocked in V60 Owner Review.
- Added a machine-readable catalogue schema, Gemini request template,
  postprocess job template and the formal production workflow.

### Visual direction

- Accepted the Owner's Sites prototype version 4 as the visual reference,
  while keeping its implementation outside the V60 Runtime authority chain.
- Established Eastern fairy-tale picture-book as the shared V60 style:
  off-white product ground, hand-painted LifeTree world, ink green and
  restrained antique-gold responses.
- Defined video-first cinematic transitions with state-safe Runtime handoffs,
  reduced-motion behavior and a reusable Gemini style-lock prompt.
- Admitted `ABU_CHARACTER_V60_V1` as the primary picture-book Abu identity,
  retained `ABU_CHARACTER_V1` as legacy compatibility, and switched the local
  V60 presentation to the new transparent seated poster.
- Rebuilt the local V60 presentation around the accepted white Eastern
  fairy-tale picture-book composition across login, LifeTree, Abu, Theater and
  Mingli Lab projections.
- Registered the prototype LifeTree scene and its clean pre-fruit derivative
  with immutable source, process and Runtime hashes.
- Removed the old semantic-tree leaf, branch, flower and foreground images
  from the active LifeTree rendering path; interaction now follows stable
  tree-attached semantic anchors.
- Verified question, Seal, world advance, fruit maturity, Reveal, Lab and
  refresh recovery in the new visual shell without document scrolling or
  pre-reveal evidence leakage.

### First return encounter

- Added an explicit second visit after the first fruit is reconciled.
- Reused the same canonical actor, persistent LifeTree, ChartVersion,
  LifeCaseRevision, world clock, Seal, Fruit and Reveal owners.
- Committed a new 砚舟 action to the canonical synthetic timeline before the
  second question becomes available.
- Added a second two-leaf / one-branch / one-flower / one-fruit semantic organ
  set and an independently sealed future WorldEvent.
- Removed first-question constants from active encounter selection, fruit
  maturity and reveal settlement.
- Made world advance, reveal and reconciliation terminal-state idempotent so
  repeated requests cannot move a completed encounter backward.
- Switched the product header from a CSS placeholder mark to the admitted V60
  transparent AbuKnows logo asset.
- Admitted `ABU_V60_SEATED_IDLE_LOOP_V1` from the Owner-approved usable source
  segment: source frames 0-53 form the transparent seated idle, while the later
  mouth-open motion is excluded. The loop uses an exact first-frame closure
  without crossfade, removes source audio and watermark space, and provides
  VP9 alpha, animated WebP and reduced-motion poster deliveries.
- Admitted `ABU_V60_05_GUIDE_LEFT_ONCE_V1` as a single leftward invitation.
  The source's repeated second gesture is excluded, the right-side generated
  window is removed before alpha extraction, and the one-shot returns to the
  V60 seated idle. Runtime deliveries include VP9 alpha, animated WebP and a
  reduced-motion open-paw poster.
- Connected `GUIDE_LEFT` to the right-side companion rail for a new Encounter
  with an unobserved tree organ. The cue plays at most once per Encounter,
  yields immediately when the viewer begins observing the tree, and returns to
  the registered V60 idle without owning any canonical state.
